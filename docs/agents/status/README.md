# Agent Status

Status docs are point-in-time snapshots. Treat them as **historical notes**, not contracts.

If a status file becomes outdated, prefer adding a fresh snapshot rather than editing old ones.
