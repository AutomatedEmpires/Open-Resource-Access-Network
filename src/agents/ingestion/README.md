# Ingestion Agent

Ingestion pipeline code for discovering, fetching, extracting, verifying, and staging candidate service records.

See `docs/agents/AGENTS_INGESTION_PIPELINE.md` for the system-level overview and safety boundaries.
