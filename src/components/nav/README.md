# Navigation Components

Navigation UI for public, seeker, organization, community volunteer, and ORAN
admin surfaces. `ScopedMobileNav` keeps the fixed bottom navigation limited to
the links for the current workspace instead of mixing roles or permissions.
