# Issue Templates

This folder contains GitHub issue template configuration for the ORAN repository.

## Notes

- Contact links in `config.yml` should point to canonical docs paths under `docs/`.
- Audience routing forms include `onboarding-help.yml` and `investor-diligence-request.yml`.
