# GitHub Workflows

This folder contains GitHub Actions workflows for CI automation.

## What to expect

- Validation workflows (lint/typecheck/tests)
- Checks that enforce repository conventions
- Runbook freshness governance checks for `docs/ops/**`

If you change scripts or tooling, ensure workflows stay aligned with `package.json` scripts.
