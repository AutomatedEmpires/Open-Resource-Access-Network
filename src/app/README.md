# Next.js App Router

Application pages, layouts, and route groups.

## Structure

- Route groups like `(seeker)` and `(host)` are used to partition role-based surfaces.
- API routes live under `src/app/api/` (see `src/app/api/README.md`).
