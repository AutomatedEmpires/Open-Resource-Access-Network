# Components

Reusable UI components for ORAN.

## Subfolders

- `ui/`: design-system primitives (see `src/components/ui/README.md`)
- Feature components grouped by area (chat, directory, map, nav, feedback)
