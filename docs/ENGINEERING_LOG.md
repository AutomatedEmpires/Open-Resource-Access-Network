# ORAN Engineering Log

This is a human-readable, append-only log for safety-critical and contract-level repository changes.

Rules:

- Use UTC timestamps (ISO 8601).
- Keep entries short.
- Link to PRs/issues when available.
- Do not include PII.

## Entries

- 2026-03-17T17:01:14Z — Extended the seeker execution slice beyond the local-first plan foundation: plan items now support local reminder scheduling, the seeker shell evaluates reminder/dashboard feature dependencies fail-closed, and a new gated `/dashboard` surface summarizes the active plan, next actions, progress, and upcoming reminder timing from local execution state without introducing server-side seeker history. Added deterministic summary helpers plus focused route/layout/dashboard coverage and kept the execution slice grounded in stored ORAN records. (src/domain/execution.ts, src/services/plans/client.ts, src/services/plans/dashboard.ts, src/app/(seeker)/plan/PlanPageClient.tsx, src/components/seeker/SeekerFeatureFlags.tsx, src/app/(seeker)/layout.tsx, src/app/(seeker)/SeekerLayoutShell.tsx, src/app/(seeker)/dashboard/page.tsx, src/app/(seeker)/dashboard/DashboardPageClient.tsx, src/services/plans/__tests__/client.test.ts, src/services/plans/__tests__/dashboard.test.ts, src/app/(seeker)/__tests__/dashboard-page.test.tsx, src/app/(seeker)/__tests__/layout-shell.test.tsx, src/app/(seeker)/README.md, docs/ENGINEERING_LOG.md)

- 2026-03-17T18:00:00Z — Completed Audit 3 (Perimeter Hardening): third comprehensive security audit covering 12 surfaces not addressed in Audits 1-2 (client-side, CSP/headers, secrets, dependencies, blob storage, OAuth/JWT, scheduled tasks, error handling, SSRF, crypto, browser storage). 4 P3 observations found, 0 critical/P1/P2. Combined 3-audit total: 24 findings, 20 resolved with code changes, 4 documented observations. Platform assessed as production-ready. (docs/audit/PERIMETER_HARDENING_AUDIT.md, docs/ENGINEERING_LOG.md)
- 2026-03-17T12:30:00Z — Added the first seeker execution substrate as a local-first plan workspace: introduced typed seeker plan objects and storage helpers, shipped a new seeker `/plan` workspace for manual steps plus saved-service imports, added deterministic “Add to plan” actions on directory and chat service cards, extended the seeker shell nav with a plan entry point, and recorded the local-first execution boundary in ADR-0013. Execution objects remain device-local in this slice and keep linked-service items anchored to stored ORAN records. (src/domain/execution.ts, src/services/plans/client.ts, src/services/plans/snapshots.ts, src/components/seeker/AddToPlanDialog.tsx, src/app/(seeker)/plan/page.tsx, src/app/(seeker)/plan/PlanPageClient.tsx, src/components/directory/ServiceCard.tsx, src/components/chat/ChatServiceCard.tsx, src/app/(seeker)/layout.tsx, src/services/plans/__tests__/client.test.ts, src/domain/constants.ts, src/services/flags/flags.ts, src/app/(seeker)/README.md, docs/DECISIONS/ADR-0013-seeker-execution-local-first-plan-foundation.md, docs/ENGINEERING_LOG.md)

- 2026-03-17T00:00:00Z — Added canonical source-link assist for unified resource submission intake: the shared resource workspace now computes completion state from the live local draft, supports analyzing a pasted source URL to suggest missing canonical fields/tags/evidence into the same submission schema, and exposes a protected `/api/resource-submissions/[id]/assist` endpoint with focused route/helper coverage. (src/services/resourceSubmissions/assist.ts, src/services/resourceSubmissions/assistShared.ts, src/components/resource-submissions/ResourceSubmissionWorkspace.tsx, src/app/api/resource-submissions/[id]/assist/route.ts, src/services/resourceSubmissions/__tests__/assistShared.test.ts, src/app/api/resource-submissions/[id]/assist/__tests__/route.test.ts, src/app/api/README.md, docs/ENGINEERING_LOG.md)

- 2026-03-16T17:50:00Z — Hardened production auth readiness for operator access: credentials sign-in now works against any `user_profiles` row carrying a password hash, including existing Microsoft Entra-backed operator profiles, and the auth layer now tolerates legacy production schemas that do not yet have `account_status`. Updated the owner bootstrap script so it can set or rotate a password on an existing Entra-backed operator without changing the underlying identity, then enabled the production credentials-auth gate and verified the live provider surface exposes both Entra and credentials sign-in. (src/lib/auth.ts, src/lib/__tests__/auth.test.ts, scripts/provision-owner-access.mjs, scripts/README.md, docs/platform/INTEGRATIONS.md, docs/platform/DEPLOYMENT_AZURE.md, docs/SECURITY_PRIVACY.md, docs/governance/ROLES_PERMISSIONS.md, docs/ops/services/RUNBOOK_AUTH_OUTAGE.md, docs/ENGINEERING_LOG.md)

- 2026-03-16T16:49:12Z — Added a privileged access bootstrap path for owner/operator setup: `scripts/provision-owner-access.mjs` now creates or upgrades credentials-backed operator accounts by email, restores them to active `oran_admin` status, provisions `host_admin` membership on a designated organization, and seeds `admin_review_profiles` with ORAN-admin capacity defaults so the same principals can enter both ORAN-admin and org/host surfaces. Added package-script and script catalog references, and explicitly preserved the truthful auth contract that phone is only a credentials identifier today, not in-app SMS/OTP 2SV. (scripts/provision-owner-access.mjs, package.json, scripts/README.md, docs/governance/ROLES_PERMISSIONS.md, docs/ENGINEERING_LOG.md)

- 2026-03-16T16:34:28Z — Followed through the governance runbooks with code-backed suspension-aware controls: host-team invites/reactivation now reject frozen users, org-claim approval refuses to promote a frozen claimant, and the two-person grant service now fails closed for frozen or unknown target users at both request and approval time. Updated the membership/scope governance runbook and the complex-flows matrix to reflect the newly verifiable state, and revalidated the touched governance suites plus `npx tsc --noEmit`. (src/app/api/host/admins/route.ts, src/app/api/admin/approvals/route.ts, src/services/workflow/two-person.ts, src/app/api/host/__tests__/host-admins-extra-coverage.test.ts, src/app/api/admin/__tests__/approvals-zones-extra.test.ts, src/services/workflow/__tests__/two-person.test.ts, docs/ops/services/RUNBOOK_ACCOUNT_AND_FORM_RESILIENCE.md, docs/ops/services/RUNBOOK_MEMBERSHIP_SCOPE_AND_REVIEWER_GOVERNANCE.md, docs/solutions/GOVERNANCE_MEMBERSHIP_ACCESS_COMPLEX_FLOWS_VII.md, docs/ENGINEERING_LOG.md)

- 2026-03-16T16:20:37Z — Implemented the next operator-governance tranche end to end: added persisted account freeze/restore state on `user_profiles` with NextAuth/session enforcement, shipped ORAN-admin account security and trust-and-safety report review APIs plus operator pages, applied integrity holds automatically when approved high-risk community reports confirm unsafe listings, and added a unified ORAN operations dashboard for pending approvals, appeals, reports, scope grants, and held services. Validation passed with focused Vitest for the new/admin/auth surfaces (8 files / 52 tests) and `npx tsc --noEmit`. The ORAN-admin account security portal now lives at `/admin-security` so the public `/security` policy page remains unchanged. (db/migrations/0053_account_status_and_security_controls.sql, src/lib/auth.ts, src/services/auth/session.ts, src/app/api/admin/security/accounts/route.ts, src/app/api/admin/reports/route.ts, src/app/api/admin/operations/summary/route.ts, src/app/(oran-admin)/admin-security/page.tsx, src/app/(oran-admin)/reports/page.tsx, src/app/(oran-admin)/operations/page.tsx, src/app/(oran-admin)/admin-security/SecurityPageClient.tsx, src/app/(oran-admin)/reports/ReportsPageClient.tsx, src/app/(oran-admin)/operations/OperationsPageClient.tsx, src/app/api/admin/security/accounts/__tests__/route.test.ts, src/app/api/admin/reports/__tests__/route.test.ts, src/app/api/admin/operations/summary/__tests__/route.test.ts, src/app/(oran-admin)/__tests__/security-page.test.tsx, src/app/(oran-admin)/__tests__/reports-page.test.tsx, src/app/(oran-admin)/__tests__/operations-page.test.tsx, src/services/auth/__tests__/session.test.ts, src/lib/__tests__/auth.test.ts, docs/ENGINEERING_LOG.md)

- 2026-03-16T09:45:00Z — Added a new 500-flow planning matrix spanning seeker experience, host/admin clarity, forms, privacy-safe data governance, HSDS/211 inbound and outbound federation, search/chat trust boundaries, accessibility, SEO, mobile, auth, notifications, complaints, security incidents, outages, portal unification, documentation retention, role boundaries, and quality/fallback design. The matrix uses explicit implemented/partial/gap labels and intentionally replaces any user-data-sale framing with consent, minimization, retention, and governed analytics language consistent with ORAN privacy constraints. (docs/solutions/OPERATIONS_EXPERIENCE_RESILIENCE_COMPLEX_FLOWS_VIII.md, docs/ENGINEERING_LOG.md)

- 2026-03-16T09:10:00Z — Closed two runbook drift points with executable repo changes: added `report:211-feed-status`, a database-backed operator CLI that summarizes 211/HSDS feed health, replay state, recent source-record activity, canonical provenance counts, and publication-decision telemetry for a source feed; and corrected runbook readiness/staleness docs to reflect the already-live CI freshness enforcement workflow instead of continuing to describe it as backlog. Added focused helper tests for the new status report script. (scripts/run-211-feed-status-report.mjs, src/__tests__/scripts/run-211-feed-status-report.test.ts, package.json, docs/ops/services/RUNBOOK_211_API_INGESTION.md, docs/ops/services/RUNBOOK_INGESTION.md, docs/ops/core/RUNBOOK_STALE_RUNBOOK_GOVERNANCE.md, docs/ops/core/OPERATIONS_READINESS.md, docs/ENGINEERING_LOG.md)

- 2026-03-16T06:49:33Z — Reworked the seeker chat surface around the white/black sitewide treatment: renamed the seeker surface to Chat, exposed saved/recent chat history more clearly, tightened resource-card CTA links to explicit ORAN/provider destinations, lowered the shared chat quota cap from 50 to 20, and hardened the deterministic out-of-scope refusal so ORAN Chat states its verified-service-only mission and declines unrelated or inappropriate requests. Updated the touched chat docs and seeker/chat regression coverage accordingly. (src/app/(seeker)/chat/ChatPageClient.tsx, src/app/(seeker)/chat/page.tsx, src/components/chat/ChatWindow.tsx, src/components/chat/ChatServiceCard.tsx, src/services/chat/orchestrator.ts, src/domain/constants.ts, src/services/chat/README.md, docs/CHAT_ARCHITECTURE.md, docs/SECURITY_PRIVACY.md, src/app/(seeker)/__tests__/chat-page-client.test.tsx, src/app/(seeker)/__tests__/wrapper-pages.test.tsx, src/components/chat/__tests__/ChatWindow.test.tsx, src/components/chat/__tests__/ChatWindow.interactions.test.tsx, src/services/chat/__tests__/intent-schema.test.ts, docs/ENGINEERING_LOG.md)

- 2026-03-16T08:05:00Z — Organized the recent signup/forms hardening work into operational runbooks and added a new governance backlog matrix for 200 complex membership, scope, reviewer-dormancy, listing-removal, onboarding, and MFA flows. The new runbooks distinguish code-backed controls from unimplemented governance gaps so the repo can be used as an honest operating reference rather than an aspirational one. (docs/ops/services/RUNBOOK_ACCOUNT_AND_FORM_RESILIENCE.md, docs/ops/services/RUNBOOK_MEMBERSHIP_SCOPE_AND_REVIEWER_GOVERNANCE.md, docs/solutions/GOVERNANCE_MEMBERSHIP_ACCESS_COMPLEX_FLOWS_VII.md, docs/ops/README.md, docs/ENGINEERING_LOG.md)

- 2026-03-16T07:20:00Z — Followed the signup/form hardening tranche with two execution-level best-practice fixes: the managed-form service now takes a transaction-scoped advisory lock and reuses an identical still-draft form instance when the same create request is retried, preventing duplicate drafts from browser retries or double-submits without collapsing distinct work; and the public credentials registration form now sends the hidden honeypot field that the hardened registration route already expects. Added focused route, service, resource-submission, and auth-page coverage and kept `tsc --noEmit` green. (src/services/forms/vault.ts, src/app/api/forms/instances/route.ts, src/services/resourceSubmissions/service.ts, src/services/forms/__tests__/vault-core.test.ts, src/app/api/forms/instances/__tests__/route.test.ts, src/services/resourceSubmissions/__tests__/service.test.ts, src/app/auth/signin/SignInPageClient.tsx, src/app/auth/__tests__/pages.test.tsx, docs/ui/FORM_FLOW_EVIDENCE_MAP.md, docs/ENGINEERING_LOG.md)

- 2026-03-16T07:00:00Z — Signup and managed-form abuse hardening tranche: registration now trims identity fields before validation, rejects invalid JSON bodies, adds a honeypot field for low-signal bot traffic, and enforces stronger password rules against common or identity-derived secrets. Managed-form creation now mirrors update-time enforcement by rejecting attachment-policy violations and oversized draft payloads at create time, and by requiring an explicit recipient role when callers route directly to a user or organization. Added focused route and service coverage plus a new 200-scenario adversarial matrix for signup and form submission abuse. (src/app/api/auth/register/route.ts, src/app/api/auth/register/__tests__/route.test.ts, src/app/api/forms/instances/route.ts, src/app/api/forms/instances/__tests__/route.test.ts, src/services/forms/vault.ts, src/services/forms/__tests__/vault-core.test.ts, docs/ui/FORM_FLOW_EVIDENCE_MAP.md, docs/solutions/SIGNUP_FORMS_ADVERSARIAL_SCENARIOS_VI.md, docs/ENGINEERING_LOG.md)

- 2026-03-16T06:05:00Z — Moved the new ingestion workforce-silence signals into live enforcement. The internal SLA/escalation job now reclaims stalled review assignments from silent reviewers by reassigning them to the next available admin, and it sends continuity/outreach alerts when active live listings sit under organizations whose host-admin coverage appears silent. Validation: focused Vitest passed (4 files / 34 tests) and `npx tsc --noEmit` passed. (src/services/escalation/engine.ts, src/services/escalation/__tests__/engine.test.ts, src/app/api/internal/sla-check/__tests__/route.test.ts, docs/agents/AGENTS_INGESTION_PIPELINE.md, docs/solutions/INGESTION_RESILIENCE_CONTROL_PLAN.md, docs/ENGINEERING_LOG.md)

- 2026-03-16T05:52:00Z — Extended the ORAN-admin ingestion health surface beyond feed silence by adding workforce-governance silence detection: the ingestion overview now reports silent reviewers with stalled assigned backlog plus owner organizations whose host-admin coverage appears inactive, and combines those signals into the existing degraded-mode recommendation shown in the admin UI. Added focused workforce-health coverage and revalidated the ingestion tranche with focused Vitest (5 files / 31 tests). Repository-wide `tsc --noEmit` was not rerun in this slice; the repo still has known unrelated seeker map type errors from the earlier tranche. (src/services/ingestion/workforceHealth.ts, src/services/ingestion/__tests__/workforceHealth.test.ts, src/app/api/admin/ingestion/overview/route.ts, src/app/api/admin/ingestion/__tests__/overview.test.ts, src/app/(oran-admin)/ingestion/IngestionPageClient.tsx, docs/agents/AGENTS_INGESTION_PIPELINE.md, docs/solutions/INGESTION_RESILIENCE_CONTROL_PLAN.md, docs/ENGINEERING_LOG.md)

- 2026-03-16T05:35:00Z — Implemented the first ingestion integrity/resilience runtime tranche: high-risk ingestion admin mutations no longer apply directly but instead queue as `ingestion_control_change` submissions requiring second approval for source trust changes, source-system trust-tier changes, source-feed auto-publish rollout, and source/source-system/source-feed deactivation. Also added feed-silence and degraded-mode assessment to the ORAN-admin ingestion overview plus focused regression coverage for the new queueing and health logic. Validation: focused Vitest passed (4 files / 28 tests). Repository-wide `tsc --noEmit` still fails on pre-existing seeker map taxonomy errors outside this change. (src/services/ingestion/controlChanges.ts, src/app/api/admin/ingestion/sources/[id]/route.ts, src/app/api/admin/ingestion/source-systems/[id]/route.ts, src/app/api/admin/ingestion/source-feeds/[id]/route.ts, src/app/api/admin/ingestion/source-feeds/bulk/route.ts, src/services/ingestion/feedHealth.ts, src/app/api/admin/ingestion/overview/route.ts, src/app/(oran-admin)/ingestion/IngestionPageClient.tsx, src/app/api/admin/ingestion/__tests__/sources.test.ts, src/app/api/admin/ingestion/__tests__/source-system-detail.test.ts, src/app/api/admin/ingestion/__tests__/overview.test.ts, src/services/ingestion/__tests__/feedHealth.test.ts, docs/agents/AGENTS_INGESTION_PIPELINE.md, docs/solutions/INGESTION_RESILIENCE_CONTROL_PLAN.md, docs/ENGINEERING_LOG.md)

- 2026-03-16T05:10:00Z — Expanded the ingestion hardening library beyond the original 300 collision/authority scenarios by adding two new 100-scenario matrices focused on adversarial abuse, fake listings, compromised actors, silent providers/admins, and degraded dependency behavior. Documented the next control tranche for ingestion integrity and operational resilience, updated the ingestion SSOT to include silence, abuse, and degraded-mode expectations, and recorded the safety-critical decision as ADR-0011. (docs/INGESTION_ADVERSARIAL_SCENARIOS_IV.md, docs/INGESTION_RESILIENCE_SCENARIOS_V.md, docs/solutions/INGESTION_RESILIENCE_CONTROL_PLAN.md, docs/DECISIONS/ADR-0011-ingestion-integrity-and-resilience-controls.md, docs/agents/AGENTS_INGESTION_PIPELINE.md, docs/ENGINEERING_LOG.md)

- 2026-03-16T04:20:00Z — Improved seeker map wayfinding and ordering: added real distance sorting to the public search contract for geo/map flows, computed viewport-center distance for bbox map searches so nearest-first ordering and distance labels remain deterministic, added visible map scale controls to both Azure Maps and the Leaflet fallback, and simplified the seeker map UI with icon-led need chips plus map-native list-order choices. (src/services/search/types.ts, src/services/search/discovery.ts, src/services/search/engine.ts, src/app/(seeker)/map/MapPageClient.tsx, src/components/map/MapContainer.tsx, src/components/map/LeafletFallback.tsx, src/services/search/README.md, docs/SCORING_MODEL.md, docs/ui/flows/seeker-discovery.md, docs/ENGINEERING_LOG.md)

- 2026-03-16T03:35:00Z — Added non-destructive update semantics across the unified publish lanes so matched live services, organizations, and locations no longer lose known-good values when incoming sources omit fields or send blanks. Extended focused regression coverage for the new preservation contract and documented a third 100-scenario ingestion matrix covering more complex adversarial, multilingual, temporal, and schema-drift publication risks. (src/services/resourceSubmissions/service.ts, src/agents/ingestion/livePublish.ts, src/agents/ingestion/promoteToLive.ts, src/services/resourceSubmissions/__tests__/service.test.ts, src/agents/ingestion/__tests__/livePublish.test.ts, src/agents/ingestion/__tests__/promoteToLive.test.ts, docs/INGESTION_COMPLEX_SCENARIOS_III.md, docs/agents/AGENTS_INGESTION_PIPELINE.md, docs/ENGINEERING_LOG.md)

- 2026-03-16T03:22:00Z — Completed the next publication-hardening pass for ingestion collisions and stale-source protection: added a shared live-publication authority policy so matched records are no longer just deduped but also protected from weaker-lane overwrites, fixed the candidate suppression path so adopted organizations are reused without duplicate inserts, added focused authority regression coverage, and documented both the reviewed first 100 scramble scenarios and a second 100 publication-accuracy challenges with recommended Azure/Microsoft/GitHub tooling for further verification and replay automation. (src/services/publication/liveAuthority.ts, src/services/publication/__tests__/liveAuthority.test.ts, src/agents/ingestion/livePublish.ts, src/agents/ingestion/promoteToLive.ts, src/services/resourceSubmissions/service.ts, src/agents/ingestion/__tests__/livePublish.test.ts, src/agents/ingestion/__tests__/promoteToLive.test.ts, src/services/resourceSubmissions/__tests__/service.test.ts, docs/INGESTION_SCRAMBLE_SCENARIOS.md, docs/INGESTION_ACCURACY_CHALLENGES_II.md, docs/agents/AGENTS_INGESTION_PIPELINE.md, docs/ENGINEERING_LOG.md)

- 2026-03-16T02:55:00Z — Hardened cross-lane live publication convergence for ingestion collisions: host-approved submissions, candidate auto-publish, and canonical promotion now share normalized live-entity matching plus a transaction-scoped advisory lock before materializing organization/service/location rows, allowing concurrent host, crawler, and feed events to reuse one live identity instead of racing into duplicates. Added focused regression coverage for duplicate adoption in the three publish lanes and documented a 100-scenario ingestion scramble matrix for further hardening work. (src/services/publication/liveEntityMerge.ts, src/services/resourceSubmissions/service.ts, src/agents/ingestion/livePublish.ts, src/agents/ingestion/promoteToLive.ts, src/services/resourceSubmissions/__tests__/service.test.ts, src/agents/ingestion/__tests__/livePublish.test.ts, src/agents/ingestion/__tests__/promoteToLive.test.ts, docs/INGESTION_SCRAMBLE_SCENARIOS.md, docs/agents/AGENTS_INGESTION_PIPELINE.md, docs/ENGINEERING_LOG.md)

- 2026-03-16T02:45:00Z — Removed the remaining direct-write host intake divergences from the ingestion system: host service creation and organization claims now enter the shared resource-submission workflow instead of creating live rows ahead of review, host-created listings reuse the same source-aware auto-publish policy as Resource Studio, and the resource-submissions collection API now returns persisted review/publication metadata including reverification timestamps instead of a hand-built null-filled summary. Also fixed the empty draft factory so nested submission cards initialize correctly for both listing and claim flows. (src/app/api/host/services/route.ts, src/app/api/host/claim/route.ts, src/app/api/resource-submissions/route.ts, src/services/resourceSubmissions/submissionExecution.ts, src/domain/resourceSubmission.ts, src/app/api/host/__tests__/host-routes.test.ts, src/app/api/resource-submissions/__tests__/route.test.ts, docs/ENGINEERING_LOG.md)

- 2026-03-16T02:10:00Z — Extended the source-aware publication model into the crawler/manual ingestion lane: allowlisted pipeline candidates now auto-publish immediately after materialization when they satisfy the existing publish-readiness threshold, while low-readiness or non-allowlisted candidates continue through the review path. Also surfaced publication outcomes in the ORAN-admin ingestion trigger response so operators can see when a run promoted directly to a live service. Added focused ingestion-service and route coverage and updated the ingestion SSOT to reflect the new candidate auto-publish rule. (src/agents/ingestion/service.ts, src/agents/ingestion/__tests__/service.test.ts, src/app/api/admin/ingestion/process/route.ts, src/app/api/admin/ingestion/__tests__/process.test.ts, docs/agents/AGENTS_INGESTION_PIPELINE.md, docs/ENGINEERING_LOG.md)

- 2026-03-16T02:35:00Z — Unified publication side effects across the candidate, canonical, and approved-submission lanes by introducing shared confidence/snapshot/lifecycle helpers and wiring deterministic reverification metadata into candidate publish records plus approved submission payloads. Approved listing projections now write confidence, HSDS snapshots, lifecycle events, and publish/reverify timestamps instead of only mutating live tables. (src/services/publication/livePublication.ts, src/agents/ingestion/livePublish.ts, src/agents/ingestion/promoteToLive.ts, src/services/resourceSubmissions/service.ts, src/services/resourceSubmissions/__tests__/service.test.ts, docs/agents/AGENTS_INGESTION_PIPELINE.md, docs/ENGINEERING_LOG.md)

- 2026-03-16T01:40:00Z — Began shifting resource publication from universal human gating toward a source-aware agentic model: host-managed listing submissions now auto-progress through `auto_checking` to `approved` on submit and project live without manual reviewer intervention, while public/community submissions continue routing to manual review. Also documented the new source-aware publication policy in the ingestion SSOT and a dedicated ADR. (src/app/api/resource-submissions/[id]/route.ts, src/app/api/resource-submissions/[id]/__tests__/route.test.ts, docs/agents/AGENTS_INGESTION_PIPELINE.md, docs/DECISIONS/ADR-0010-source-aware-agentic-publication-policy.md, docs/ENGINEERING_LOG.md)

- 2026-03-13T22:50:00Z — Added operator-facing publication denial telemetry to the source-feed poller: `feed.poll_completed` audit outputs and persisted `lastAttemptSummary` now include `publicationReason` plus aggregated `decisionReasons`, allowing operators to distinguish explicit approval-gate fallback, missing required locations, and policy-threshold filtering during 211 rollout analysis. Added focused ingestion-service assertions for the new observability surface. (src/agents/ingestion/service.ts, src/agents/ingestion/__tests__/service.test.ts, docs/ops/services/RUNBOOK_211_API_INGESTION.md, docs/ENGINEERING_LOG.md)

- 2026-03-13T22:45:00Z — Tightened Phase 3 trust controls for 211 auto-publish by adding a distinct `trusted_partner` confidence floor in the auto-publish policy engine and wiring the ingestion service to pass an explicit `ORAN_TRUSTED_PARTNER_AUTO_PUBLISH_MIN_CONFIDENCE` threshold (default `90`). Added focused policy/runtime regression coverage and updated the 211 rollout runbook to document that feed approval alone is insufficient for `trusted_partner` auto-publish without the higher confidence bar. (src/agents/ingestion/autoPublish.ts, src/agents/ingestion/service.ts, src/agents/ingestion/__tests__/autoPublish.test.ts, src/agents/ingestion/__tests__/service.test.ts, docs/ops/services/RUNBOOK_211_API_INGESTION.md, docs/ENGINEERING_LOG.md)

- 2026-03-13T22:35:00Z — Hardened 211 Phase 3 publication controls so `auto_publish` now requires an explicit per-feed approval stamp recorded in `source_feed_states`, with approving actor/time persisted and automatically cleared when rollout scope changes. Updated the ingestion runtime to fail closed back to review queueing when the approval stamp is missing, extended the ORAN admin ingestion UI/API to set and display approval state, and added focused service/admin/UI regression coverage. (db/migrations/0047_source_feed_auto_publish_approval.sql, src/db/schema.ts, src/agents/ingestion/persistence/sourceFeedStateStore.ts, src/app/api/admin/ingestion/source-feeds/state.ts, src/app/api/admin/ingestion/source-feeds/[id]/route.ts, src/app/api/admin/ingestion/source-feeds/bulk/route.ts, src/agents/ingestion/service.ts, src/agents/ingestion/__tests__/service.test.ts, src/app/api/admin/ingestion/__tests__/source-system-detail.test.ts, src/app/(oran-admin)/ingestion/IngestionPageClient.tsx, src/app/(oran-admin)/__tests__/ingestion-page.test.tsx, docs/ops/services/RUNBOOK_211_API_INGESTION.md, docs/ENGINEERING_LOG.md)

- 2026-03-13T22:15:00Z — Added staging-canary execution tooling for the 211 rollout: introduced a `report:211-canary` script that queries persisted source records, canonical provenance, and canonical entities to produce a canary reconciliation artifact with normalization coverage, publication posture, top errors, and sampled source-versus-canonical comparisons; added focused helper tests for the report parser/reconciliation logic; and updated the 211 ingestion runbook with the staging-canary command and artifact-signoff sequence. Validation passed (`src/__tests__/scripts/run-211-canary-report.test.ts`, 3 tests; `tsc --noEmit`). (scripts/run-211-canary-report.mjs, src/__tests__/scripts/run-211-canary-report.test.ts, package.json, docs/ops/services/RUNBOOK_211_API_INGESTION.md, docs/ENGINEERING_LOG.md)

- 2026-03-13T22:00:00Z — Completed the remaining source-feed operator workflows for nationwide rollout: source-feed state updates now preserve checkpoint and sync metadata instead of resetting them, added admin APIs for single-feed replay-from-checkpoint and bulk feed-state updates, and extended the ORAN admin ingestion page with bulk pause/review/replay controls plus per-feed replay actions. Focused admin/ingestion Vitest passed (2 files / 16 tests) and `tsc --noEmit` passed. (src/app/api/admin/ingestion/source-feeds/state.ts, src/app/api/admin/ingestion/source-feeds/[id]/route.ts, src/app/api/admin/ingestion/source-feeds/[id]/replay/route.ts, src/app/api/admin/ingestion/source-feeds/bulk/route.ts, src/app/api/admin/ingestion/__tests__/source-system-detail.test.ts, src/app/(oran-admin)/ingestion/IngestionPageClient.tsx, src/app/(oran-admin)/__tests__/ingestion-page.test.tsx, docs/ops/services/RUNBOOK_211_API_INGESTION.md, docs/ENGINEERING_LOG.md)

- 2026-03-13T21:55:00Z — Extended the ORAN admin ingestion control plane so rollout-state management is no longer bootstrap-only: the source-feed detail API now reads and updates persisted feed state, and the admin ingestion page now lets operators toggle source/feed activation, set per-feed publication mode, emergency pause a feed, constrain included/excluded 211 data owners, set max organizations per poll, and record rollout notes. Focused ingestion/admin Vitest passed (8 files / 58 tests) and `tsc --noEmit` passed. (src/app/api/admin/ingestion/source-feeds/[id]/route.ts, src/app/api/admin/ingestion/__tests__/source-system-detail.test.ts, src/app/(oran-admin)/ingestion/IngestionPageClient.tsx, docs/ops/services/RUNBOOK_211_API_INGESTION.md, docs/ENGINEERING_LOG.md)

- 2026-03-13T21:45:00Z — Completed the remaining engineering substrate for Phase 2-4 of the 211 API ingestion rollout: added durable `source_feed_states` rollout/sync persistence and Drizzle store wiring, stamped `winningSourceSystemId` during normalization, wired feed-state-aware publication routing (`canonical_only`, `review_required`, controlled `auto_publish`) and checkpoint/replay handling into the source-feed poll loop, added source-system/source-feed detail APIs for update/deactivate, and reconciled the ingestion test surface with the new dual-fetch admin bootstrap and feed lookup dependency. Targeted ingestion validation passed (12 Vitest files / 95 tests). (db/migrations/0046_source_feed_operational_state.sql, src/db/schema.ts, src/agents/ingestion/stores.ts, src/agents/ingestion/persistence/sourceFeedStateStore.ts, src/agents/ingestion/persistence/storeFactory.ts, src/agents/ingestion/persistence/index.ts, src/agents/ingestion/persistence/sourceFeedStore.ts, src/agents/ingestion/service.ts, src/agents/ingestion/normalizeSourceRecord.ts, src/agents/ingestion/ndp211Connector.ts, src/app/api/admin/ingestion/source-systems/[id]/route.ts, src/app/api/admin/ingestion/source-feeds/[id]/route.ts, src/agents/ingestion/__tests__/ndp211Normalizer.test.ts, src/app/(oran-admin)/__tests__/ingestion-page.test.tsx, docs/ops/services/RUNBOOK_211_API_INGESTION.md, docs/ENGINEERING_LOG.md)

- 2026-03-13T21:10:00Z — Completed Phase 1 of the 211 API ingestion rollout: expanded the ORAN admin bootstrap surface for pollable source systems and feeds with operational metadata, added an internal scheduled feed-poll route plus hourly Azure Function timer, introduced fail-closed web-runtime gating for 211 polling via env contract checks, and added an idempotent source/feed bootstrap script for staging and production seeding. Also started Phase 2 by adding feed-poll and normalization audit events plus monitoring queries for the new poller. (src/app/(oran-admin)/ingestion/IngestionPageClient.tsx, src/app/api/admin/ingestion/source-systems/route.ts, src/app/api/internal/ingestion/feed-poll/route.ts, functions/pollSourceFeeds/index.ts, functions/pollSourceFeeds/function.json, src/services/runtime/envContractCore.js, scripts/bootstrap-source-feed.mjs, src/agents/ingestion/service.ts, src/agents/ingestion/contracts.ts, docs/ops/monitoring/MONITORING_QUERIES.md, docs/ops/services/RUNBOOK_211_API_INGESTION.md, docs/ENGINEERING_LOG.md)

- 2026-03-13T20:30:00Z — Added a dedicated operational rollout runbook for 211 API ingestion covering phased bootstrap, staging/production runtime parity, publication-policy gating, and incremental sync strategy, and registered it in the ops catalog, readiness matrix, and ingestion contract references. (docs/ops/services/RUNBOOK_211_API_INGESTION.md, docs/ops/README.md, docs/ops/core/OPERATIONS_READINESS.md, docs/contracts/INGESTION_CONTRACT.md, docs/ENGINEERING_LOG.md)

- 2026-03-13T19:00:00Z — Source-feed handler contract pass: added explicit `feed_handler` persistence on `source_feeds`, switched ingestion-service source-feed dispatch from URL/name heuristics to the stored handler contract, introduced pending-only `listPendingByFeed()` reads so normalization no longer scans entire feed history, and updated manual submission / host-portal feed creation paths to stamp non-polled feeds as `feed_handler='none'`. Targeted ingestion validation passed (`tsc --noEmit`, 5 Vitest files / 48 tests). (db/migrations/0045_add_source_feed_handler.sql, src/db/schema.ts, src/agents/ingestion/service.ts, src/agents/ingestion/persistence/sourceRecordStore.ts, src/agents/ingestion/stores.ts, src/services/resourceSubmissions/service.ts, src/services/ingestion/hostPortalIntake.ts, src/agents/ingestion/__tests__/service.test.ts, src/agents/ingestion/persistence/__tests__/sourceFeedStore.test.ts, src/agents/ingestion/persistence/__tests__/sourceRecordStore.test.ts, src/agents/ingestion/__tests__/hsdsFeedConnector.test.ts, src/agents/ingestion/__tests__/ndp211Connector.test.ts, docs/ENGINEERING_LOG.md)

- 2026-03-13T18:40:00Z — 211 source-feed runtime completion pass: wired `pollFeeds()` to normalize pending source records immediately after successful source-feed polling, dispatching 211 NDP records through `normalize211SourceRecord` with taxonomy crosswalk enabled and other source records through the generic normalizer, so the 211-specific enrichment path now runs in the live ingestion service instead of existing only in tests. Also made the 211 connector honor `NDP_211_DATA_OWNERS` from runtime env by default and stopped counting unsupported source-feed families as service-polled. Full test suite passed after the change. (src/agents/ingestion/service.ts, src/agents/ingestion/__tests__/service.test.ts, docs/ENGINEERING_LOG.md)

- 2026-03-13T08:30:00Z — Final live review-queue terminology pass: cleaned the last remaining user-facing “Back to Queue” strings in the community review workspace, aligned the community service README with the submissions-backed review model, and updated the public changelog’s community-admin feature label so live product language no longer teaches the legacy queue name. (src/app/(community-admin)/verify/VerifyPageClient.tsx, src/services/community/README.md, src/app/(public)/changelog/page.tsx, docs/ENGINEERING_LOG.md)

- 2026-03-13T08:45:00Z — Confidence and workflow doc contract cleanup: updated the live confidence-scoring SSOT to remove the retired public `minConfidence` alias from `/api/search`, reframed the early `verification_queue` data-model entry as historical rather than canonical, and cleaned the remaining live review-workflow wording in evidence maps, runbooks, and community queue route tests. (docs/solutions/CONFIDENCE_SCORING.md, docs/DATA_MODEL.md, docs/ui/FORM_FLOW_EVIDENCE_MAP.md, docs/ops/services/RUNBOOK_DATA_QUALITY_INCIDENT.md, src/app/api/community/__tests__/routes.test.ts, docs/ENGINEERING_LOG.md)

- 2026-03-13T08:55:00Z — Agent activation terminology cleanup: updated contributor-facing roadmap and activation docs so community-admin workflows are described as a submissions-backed review queue rather than the legacy verification queue, and corrected the schema-audit guidance to point at `submissions` indexes instead of the old table. (docs/agents/ROADMAP.md, docs/agents/activation/AGENT_APEX_ACTIVATION.md, docs/agents/activation/AGENT_DELTA_ACTIVATION.md, docs/ENGINEERING_LOG.md)

- 2026-03-13T09:05:00Z — SQL prompt and drift-register cleanup: corrected the SQL migration prompt so current host/community workflow guidance points at `submissions` instead of `verification_queue`, and updated the platform drift register to reflect that most remaining queue-language references are now compatibility/history or internal prompt residue rather than live product docs. (docs/agents/prompts/AGENT_PROMPT_SQL.md, docs/platform/LEGACY_RETIREMENT_MATRIX.md, docs/platform/PLATFORM_ARCHITECTURE.md, docs/ENGINEERING_LOG.md)

- 2026-03-13T09:15:00Z — Azure dashboard terminology cleanup: updated the Azure dashboard modernization plan so its recommended executive tiles and operations panels use the current “review queue backlog” wording instead of the retired “verification queue backlog” label. Re-checked the remaining legacy queue references and left only explicit compatibility/history mentions in place. (docs/platform/AZURE_DASHBOARD_MODERNIZATION.md, docs/ENGINEERING_LOG.md)

- 2026-03-16T10:30:00Z — Public homepage and changelog terminology cleanup: renamed the live homepage partner feature from “structured verification queue” to “structured review queue” and clarified the public changelog’s May 2025 schema note so `verification_queue` is presented as historical initial schema context rather than current workflow truth. (src/app/page.tsx, src/app/(public)/changelog/page.tsx, docs/ENGINEERING_LOG.md)

- 2026-03-14T18:10:00Z — 211 NDP ingestion pipeline audit (Phase 2): fixed CRITICAL `phone` field data loss (missing from SERVICE_FIELDS in normalizeSourceRecord.ts — connector put phone in payload but mapFields silently dropped it), fixed CRITICAL org_bundle enrichment bug (eligibility/cost/language tags were completely broken for organization_bundle records — code looked for `_211_eligibility` at top level but bundles nest under `services[].eligibility`; added `extractPerServiceEnrichments()` with per-service index-matched enrichment loop), extracted shared connector utilities (sha256/stableStringify/isTransient/buildUrl) from ndp211Connector.ts and hsdsFeedConnector.ts into connectorUtils.ts eliminating ~40 lines of duplication, added migration 0044 for `curated` trust tier in DB CHECK constraint, removed unused `_type` destructuring. Added ndp211Normalizer.test.ts (20+ tests for enrichment logic) and connectorUtils.test.ts (13 tests for shared utilities). 845/845 ingestion tests pass, tsc clean. (src/agents/ingestion/connectorUtils.ts, src/agents/ingestion/ndp211Connector.ts, src/agents/ingestion/ndp211Normalizer.ts, src/agents/ingestion/hsdsFeedConnector.ts, src/agents/ingestion/normalizeSourceRecord.ts, src/agents/ingestion/__tests__/connectorUtils.test.ts, src/agents/ingestion/__tests__/ndp211Normalizer.test.ts, db/migrations/0044_add_curated_trust_tier.sql, docs/ENGINEERING_LOG.md)

- 2026-03-13T08:20:00Z — Community review surface terminology cleanup: renamed the remaining live community-admin queue labels from “verification queue” to “review queue” across the UI shell, page metadata, and API route docs so the product language matches the submissions-backed review model already used by the runtime. (src/app/(community-admin)/README.md, src/app/(community-admin)/queue/page.tsx, src/app/(community-admin)/queue/QueuePageClient.tsx, src/app/(community-admin)/verify/VerifyPageClient.tsx, src/app/(community-admin)/dashboard/DashboardPageClient.tsx, src/app/(community-admin)/__tests__/queue-page.test.tsx, src/app/(community-admin)/__tests__/verify-page.test.tsx, src/app/(community-admin)/__tests__/dashboard-page.test.tsx, src/app/api/README.md, docs/ENGINEERING_LOG.md)

- 2026-03-13T08:10:00Z — Workflow-language cleanup pass: updated the highest-signal live governance, security, and UI architecture docs so community review is described through the canonical `submissions` workflow model rather than as an independent `verification_queue` lifecycle, while preserving explicit mention of the legacy compatibility view where it still exists at the schema layer. (docs/platform/PLATFORM_ARCHITECTURE.md, docs/governance/ROLES_PERMISSIONS.md, docs/SECURITY_PRIVACY.md, docs/ui/UI_SURFACE_MAP.md, docs/ui/UI_SHELL_SPEC.md, docs/ui/UX_FLOWS.md, docs/ENGINEERING_LOG.md)

- 2026-03-13T07:55:00Z — Public search contract cleanup: retired the legacy `minConfidence` query alias from `/api/search`, leaving `minConfidenceScore` as the only public confidence-floor parameter, updated the route tests accordingly, and cleared that compatibility item from the platform retirement matrix. (src/app/api/search/route.ts, src/app/api/search/__tests__/route.test.ts, docs/contracts/RESOURCE_DISTRIBUTION_API.md, docs/platform/LEGACY_RETIREMENT_MATRIX.md, docs/ENGINEERING_LOG.md)

- 2026-03-14T06:00:00Z — 211 NDP ingestion pipeline audit (Phase 1): fixed CRITICAL DB CHECK constraint mismatch (`organization_bundle`, `service_at_location` not in allowed `source_record_type` values — every INSERT would fail at runtime), fixed taxonomy FK UUID mismatch (was using 211 external ID instead of DB-generated UUID for `source_record_taxonomy.source_record_id`), simplified `isTransient()` retry logic, narrowed `service.ts` routing from all `partner_api` feeds to 211-specific feeds only, added `trusted_partner` and `blocked` to `TRUST_TIER_CONFIDENCE` map, implemented `reshape211Payload` to transform camelCase 211 bundle fields into HSDS-aligned snake_case keys for proper normalization. Added taxonomy UUID integrity test. (db/migrations/0043_extend_source_record_types.sql, src/agents/ingestion/ndp211Connector.ts, src/agents/ingestion/ndp211Normalizer.ts, src/agents/ingestion/normalizeSourceRecord.ts, src/agents/ingestion/service.ts, src/agents/ingestion/__tests__/ndp211Connector.test.ts, docs/ENGINEERING_LOG.md)

- 2026-03-13T06:05:00Z — Production Azure auth/maps completion pass: verified the prod database already had migration `0042_user_profile_usernames.sql`, added scheduled/manual Azure Maps SAS rotation automation so the live App Service setting can be refreshed and verified without manual CLI intervention, and updated Azure deployment/infrastructure docs to point at the new rotation path. (scripts/azure/rotate-maps-sas.sh, .github/workflows/rotate-azure-maps-sas.yml, docs/platform/DEPLOYMENT_AZURE.md, scripts/azure/README.md, infra/README.md, docs/ENGINEERING_LOG.md)

- 2026-03-13T07:05:00Z — Public read-path unification pass: introduced shared published-record query primitives for service and organization list/detail distribution, refactored the HSDS routes onto that read model, and aligned public search and batch service lookup with the same publication predicate so seeker discovery, ID lookup, and HSDS distribution no longer drift independently on active/published filtering. (src/services/search/publication.ts, src/services/search/types.ts, src/services/search/engine.ts, src/services/chat/retrievalProfile.ts, src/app/api/search/route.ts, src/app/api/hsds/services/route.ts, src/app/api/hsds/services/[id]/route.ts, src/app/api/hsds/organizations/route.ts, src/app/api/hsds/organizations/[id]/route.ts, src/services/search/README.md, docs/contracts/RESOURCE_DISTRIBUTION_API.md, src/app/api/hsds/README.md, docs/ENGINEERING_LOG.md)

- 2026-03-13T06:05:00Z — Production Azure auth/maps completion pass: verified the prod database already had migration `0042_user_profile_usernames.sql`, added scheduled/manual Azure Maps SAS rotation automation so the live App Service setting can be refreshed and verified without manual CLI intervention, and updated Azure deployment/infrastructure docs to point at the new rotation path. (scripts/azure/rotate-maps-sas.sh, .github/workflows/rotate-azure-maps-sas.yml, docs/platform/DEPLOYMENT_AZURE.md, scripts/azure/README.md, infra/README.md, docs/ENGINEERING_LOG.md)

- 2026-03-13T05:55:00Z — Auth expansion follow-on pass: broadened credentials auth from email-only to email/username/phone identifier login, added username persistence and uniqueness checks for credentials registration, gated Apple OAuth behind explicit production env flags, and documented the updated provider contract and outage runbook expectations. Focused auth, registration, schema, and sign-in page validation passed. (src/lib/auth.ts, src/app/api/auth/register/route.ts, src/app/auth/signin/SignInPageClient.tsx, src/app/auth/__tests__/pages.test.tsx, src/lib/__tests__/auth.test.ts, src/app/api/auth/register/__tests__/route.test.ts, src/db/schema.ts, db/migrations/0042_user_profile_usernames.sql, docs/SECURITY_PRIVACY.md, docs/platform/INTEGRATIONS.md, docs/platform/DEPLOYMENT_AZURE.md, docs/ops/services/RUNBOOK_AUTH_OUTAGE.md, docs/ENGINEERING_LOG.md)

- 2026-03-13T06:45:00Z — Platform boundary follow-on pass: made `src/agents/ingestion/**` the explicitly documented canonical ingestion domain, narrowed `src/services/ingestion/**` to a helper-layer contract, defined the public resource-distribution API tiers across `/api/search`, `/api/services`, and `/api/hsds/**`, and removed the remaining ingestion-service lint drift by moving feed poller imports to runtime-scoped loading inside the feed-poll loop. (src/agents/ingestion/service.ts, src/agents/ingestion/README.md, src/services/ingestion/README.md, docs/contracts/RESOURCE_DISTRIBUTION_API.md, src/app/api/README.md, src/app/api/hsds/README.md, docs/platform/PLATFORM_ARCHITECTURE.md, docs/DECISIONS/ADR-0009-platform-pillars-and-subsystem-boundaries.md, docs/SSOT.md, docs/ENGINEERING_LOG.md)

- 2026-03-13T06:15:00Z — Platform architecture stewardship pass: established a canonical platform-pillar map for ORAN, documented the live subsystem boundaries across ingestion/federation, discovery, governance, participation, and delivery, aligned the SSOT and repository map to that architecture, updated the search service contract to distinguish the default SQL path from non-default hybrid utilities, and reconciled ingestion workflow docs with the universal submissions model. (docs/platform/PLATFORM_ARCHITECTURE.md, docs/DECISIONS/ADR-0009-platform-pillars-and-subsystem-boundaries.md, docs/DECISIONS/README.md, docs/SSOT.md, docs/REPO_MAP.md, src/services/README.md, src/services/search/README.md, docs/agents/AGENTS_INGESTION_PIPELINE.md, docs/ENGINEERING_LOG.md)

- 2026-03-13T05:10:00Z — Production-hardening follow-on pass: expanded Redis-backed shared rate limiting across the core admin/operator APIs (approvals, appeals, audit, scopes/grants, zones, templates, triage, embeddings, capacity, control-plane, merge, and ingestion routes) while keeping test mocks compatible by exporting both limiter functions in the affected admin route and test suites. Added a committed runtime-readiness contract file plus a strict CI/App Service deploy gate that now fails when `REDIS_URL`, `AZURE_MAPS_KEY`, or `AZURE_MAPS_SAS_TOKEN` are missing from the enforced production webapp settings set. Focused validation passed for 24 admin/runtime test files (223 tests). (admin API route/test suites, scripts/validate-runtime-env.mjs, .github/runtime/webapp-production-settings.txt, .github/workflows/ci.yml, .github/workflows/deploy-azure-appservice.yml, docs/platform/DEPLOYMENT_AZURE.md, reports/production-readiness/agent_security_compliance_report.md, docs/ENGINEERING_LOG.md)

- 2026-03-13T04:45:00Z — Production-hardening expansion pass closed: extended Redis-backed shared rate limiting to additional seeker/public endpoints (`/api/search`, `/api/feedback`, `/api/profile`, `/api/saved`, `/api/services`, and seeker submission read/write routes), aligned the broader submission-route test mocks with the shared limiter contract, and revalidated the full repository gate with 318 test files / 3121 tests passing. Refreshed the production-readiness report to record the now-green repo-wide validation state. (src/app/api/search/route.ts, src/app/api/feedback/route.ts, src/app/api/profile/route.ts, src/app/api/saved/route.ts, src/app/api/services/route.ts, src/app/api/submissions/report/route.ts, src/app/api/submissions/appeal/route.ts, src/app/api/submissions/denied/route.ts, src/app/api/search/__tests__/route.test.ts, src/app/api/feedback/__tests__/route.test.ts, src/app/api/profile/__tests__/route.test.ts, src/app/api/saved/__tests__/route.test.ts, src/app/api/services/__tests__/route.test.ts, src/app/api/submissions/report/__tests__/route.test.ts, src/app/api/submissions/appeal/__tests__/route.test.ts, src/app/api/submissions/denied/__tests__/route.test.ts, src/app/api/submissions/__tests__/report-routes.test.ts, src/app/api/submissions/__tests__/appeal-routes.test.ts, reports/production-readiness/agent_security_compliance_report.md, docs/ENGINEERING_LOG.md)

- 2026-03-13T04:35:00Z — Production-hardening pass for Azure bootstrap parity and abuse controls: upgraded `scripts/azure/bootstrap.sh` to provision Azure Maps and wire both `AZURE_MAPS_KEY` and a caller-supplied `AZURE_MAPS_SAS_TOKEN` into App Service through Key Vault, and introduced Redis-backed shared rate limiting for the highest-value public/auth/privacy endpoints with in-memory fallback preserved for resilience. Updated supporting docs and the production-readiness report to reflect the new deployment and security posture. (scripts/azure/bootstrap.sh, scripts/azure/README.md, src/services/cache/redis.ts, src/services/security/rateLimit.ts, src/services/security/__tests__/rateLimit.test.ts, src/app/api/maps/token/route.ts, src/app/api/auth/register/route.ts, src/app/api/user/security/password/route.ts, src/app/api/user/data-export/route.ts, src/app/api/user/data-delete/route.ts, src/app/api/reports/route.ts, docs/SECURITY_PRIVACY.md, docs/platform/INTEGRATIONS.md, docs/platform/DEPLOYMENT_AZURE.md, reports/production-readiness/agent_security_compliance_report.md, docs/ENGINEERING_LOG.md)

- 2026-03-13T01:45:00Z — Azure Maps infrastructure codification pass: extended `infra/main.bicep` to provision an Azure Maps Gen2 account, store the generated server key in Key Vault, accept the browser SAS token as a secure deployment parameter, and wire both values into the web app through Key Vault references. Updated the production Bicep parameter guidance, Azure deployment guide, infrastructure README, and the security/compliance readiness report to match the new deployment contract. (infra/main.bicep, infra/main.prod.bicepparam, docs/platform/DEPLOYMENT_AZURE.md, infra/README.md, reports/production-readiness/agent_security_compliance_report.md, docs/ENGINEERING_LOG.md)

- 2026-03-13T00:10:00Z — Azure deployment-readiness alignment pass: updated the agent control-plane so Azure Maps health requires both the server geocoding key and the browser SAS token, corrected deployment documentation to include `AZURE_MAPS_SAS_TOKEN` and production auth-provider gates, and narrowed the infrastructure README so it no longer overclaims Azure Maps provisioning that the current Bicep template does not perform. Refreshed the production-readiness security/compliance report to capture the new readiness gap and current validation posture. (src/services/agentic/controlPlane.ts, src/services/agentic/__tests__/controlPlane.test.ts, docs/platform/DEPLOYMENT_AZURE.md, infra/README.md, reports/production-readiness/agent_security_compliance_report.md, docs/ENGINEERING_LOG.md)

- 2026-03-12T12:20:00Z — Continued security/compliance remediation: removed the remaining legacy `audit_log` writes from the deprecated seeker report endpoint and duplicate-merge service so audit persistence now uses the canonical `audit_logs` schema across the security-sensitive paths touched in this pass. Added a production-readiness security/compliance report with verified fixes, residual risks, and validation evidence. (src/app/api/reports/route.ts, src/app/api/reports/__tests__/route.test.ts, src/services/merge/service.ts, reports/production-readiness/agent_security_compliance_report.md, docs/ENGINEERING_LOG.md)

- 2026-03-12T12:00:00Z — Security/compliance hardening pass: removed raw Azure Maps shared-key exposure from `/api/maps/token` in favor of brokered SAS-token client auth, updated the web map to consume scoped auth and fail over to the Leaflet fallback when secure client auth is unavailable, gated optional Google and email/password auth so they fail closed in production unless explicitly enabled, and corrected privacy-route drift by moving user data export/delete flows to the canonical `audit_logs` schema, per-user rate-limit keys, and `Cache-Control: private, no-store` responses. Updated security and integration docs to match the enforced contract. (src/app/api/maps/token/route.ts, src/components/map/MapContainer.tsx, src/components/map/LeafletFallback.tsx, src/lib/auth.ts, src/app/auth/signin/SignInPageClient.tsx, src/app/api/auth/register/route.ts, src/app/api/user/security/password/route.ts, src/app/api/user/data-export/route.ts, src/app/api/user/data-delete/route.ts, src/app/api/maps/token/__tests__/route.test.ts, src/components/map/__tests__/MapContainer.test.tsx, src/lib/__tests__/auth.test.ts, src/app/api/auth/register/__tests__/route.test.ts, src/app/api/user/security/password/__tests__/route.test.ts, src/app/api/user/data-delete/__tests__/route.test.ts, docs/SECURITY_PRIVACY.md, docs/platform/INTEGRATIONS.md, docs/ENGINEERING_LOG.md)

- 2026-03-09T19:28:11Z — Seeker chat P2 response-quality pass: chat retrieval now pulls a larger deterministic candidate pool and diversifies the final visible set across organizations when the top-ranked results are overly concentrated, without changing trust-first ordering. Added deterministic `resultSummary` metadata to explain why a result set was shown, adaptive `followUpSuggestions` chips for successful and no-match turns, and focused regression coverage for diversified result sets and follow-up UI rendering. Updated chat architecture docs, service README, and the seeker chatbot gap-analysis audit to mark P2 complete. (src/services/chat/types.ts, src/services/chat/orchestrator.ts, src/app/api/chat/route.ts, src/components/chat/ChatWindow.tsx, src/services/chat/__tests__/intent-schema.test.ts, src/app/api/chat/__tests__/route.test.ts, src/components/chat/__tests__/ChatWindow.test.tsx, docs/CHAT_ARCHITECTURE.md, src/services/chat/README.md, docs/audit/AUDIT_SEEKER_CHATBOT_GAP_ANALYSIS.md, docs/ENGINEERING_LOG.md)

- 2026-03-09T19:18:57Z — Seeker chat P1 deterministic follow-up pass: added structured session-scoped chat context (`sessionContext`) so ambiguous follow-up turns can inherit active need/city/filter scope without storing transcripts, introduced deterministic clarification responses for weak general queries, made crisis routing subject-aware so third-party or informational crisis language gets immediate 911/988 guidance without falsely tripping the self-harm hard gate, and added quota handoff controls to Directory, Map, and one-click fresh-session restart with carried scope. Updated focused route/orchestrator/chat-window regression coverage and synchronized chat architecture docs + service README. (src/services/chat/types.ts, src/services/chat/orchestrator.ts, src/app/api/chat/route.ts, src/components/chat/ChatWindow.tsx, src/services/chat/__tests__/intent-schema.test.ts, src/app/api/chat/__tests__/route.test.ts, src/components/chat/__tests__/ChatWindow.test.tsx, src/components/chat/__tests__/ChatWindow.interactions.test.tsx, docs/CHAT_ARCHITECTURE.md, src/services/chat/README.md, docs/audit/AUDIT_SEEKER_CHATBOT_GAP_ANALYSIS.md, docs/ENGINEERING_LOG.md)

- 2026-03-09T05:32:41Z — Seeker chat P0 gap-closure pass: added explicit chat retrieval outcomes (`results`, `no_match`, `catalog_empty_for_scope`, `temporarily_unavailable`, `out_of_scope`), deterministic off-topic scope guarding, per-request profile-shaping override via `profileMode`, and response-level search interpretation disclosure so the signed-in seeker UI can show why results were ordered and let users ignore saved profile signals without dropping active browse filters. Updated the seeker chat UI, route/orchestrator contracts, targeted docs, and focused Vitest coverage for route, orchestrator, and chat-window flows. (src/services/chat/types.ts, src/services/chat/orchestrator.ts, src/app/api/chat/route.ts, src/components/chat/ChatWindow.tsx, src/services/chat/__tests__/intent-schema.test.ts, src/app/api/chat/__tests__/route.test.ts, src/components/chat/__tests__/ChatWindow.test.tsx, src/components/chat/__tests__/ChatWindow.interactions.test.tsx, docs/CHAT_ARCHITECTURE.md, src/services/chat/README.md, docs/ENGINEERING_LOG.md)

- 2026-03-08T23:15:00Z — Managed-form contract enforcement + ingestion source UI reconciliation: enforced storage-scope anchors for managed-form instance creation, added an authenticated coverage-zone listing endpoint so host users can launch community-scoped templates in-app, aligned community-admin managed-form visibility with canonical community scope, promoted template field validation, attachment rules, queue semantics, and submit confirmations into backend enforcement, and added an ORAN-admin source-create form to the ingestion Sources tab so the existing source upsert API is no longer UI-orphaned. Added focused route coverage for community zone enforcement, manual review queueing, and no-auto-queue submit behavior. (src/domain/forms.ts, src/services/forms/vault.ts, src/app/api/forms/zones/route.ts, src/app/api/forms/instances/route.ts, src/app/api/forms/instances/[id]/route.ts, src/components/forms/FormVaultWorkspace.tsx, src/app/(oran-admin)/ingestion/IngestionPageClient.tsx, src/app/api/forms/instances/__tests__/route.test.ts, src/app/api/forms/instances/[id]/__tests__/route.test.ts, docs/ui/FORM_FLOW_EVIDENCE_MAP.md, docs/ENGINEERING_LOG.md)

- 2026-03-08T22:45:00Z — Managed form vault operations upgrade: aligned managed forms with the platform submission contract so form instances now surface priority, assignee, SLA, and lifecycle timestamps; template authors can define routing defaults such as reviewer role, default urgency, and review timer; submit now stamps operational metadata for review, and reviewer start-review auto-assigns ownership. The shared `/forms` workspace now exposes reference IDs, next-action guidance, reviewer target, priority, and SLA urgency cues for host, community-admin, and ORAN-admin operators. (src/services/forms/vault.ts, src/app/api/forms/instances/[id]/route.ts, src/components/forms/FormVaultWorkspace.tsx, src/app/api/forms/instances/[id]/__tests__/route.test.ts, src/app/api/forms/instances/__tests__/route.test.ts, docs/ui/FORM_FLOW_EVIDENCE_MAP.md, docs/ENGINEERING_LOG.md)

- 2026-03-08T22:20:00Z — Managed form vault end-to-end wiring: added host, community-admin, and ORAN-admin `/forms` portal surfaces on top of the existing submission-backed form-vault APIs, exposed template schema and instructions on accessible form instances so forms can be filled and reviewed entirely in-app, added focused list/create route coverage for `/api/forms/templates` and `/api/forms/instances`, and updated the form evidence map to document the new operator surfaces. (src/components/forms/FormVaultWorkspace.tsx, src/app/(host)/forms/page.tsx, src/app/(community-admin)/forms/page.tsx, src/app/(oran-admin)/forms/page.tsx, src/app/(host)/HostLayoutShell.tsx, src/app/(community-admin)/CommunityAdminLayoutShell.tsx, src/app/(oran-admin)/OranAdminLayoutShell.tsx, src/app/api/forms/templates/__tests__/route.test.ts, src/app/api/forms/instances/__tests__/route.test.ts, src/domain/forms.ts, src/services/forms/vault.ts, docs/ui/FORM_FLOW_EVIDENCE_MAP.md, docs/ENGINEERING_LOG.md)

- 2026-03-08T21:29:00Z — Managed form vault workflow hardening: documented submission-backed `form_templates` and `form_instances` in the data model, fixed managed-form submit so instances advance from `draft` to `submitted` to `needs_review` before reviewer actions, and added focused route coverage for submit queueing, reviewer start-review, and reviewer-role enforcement. Focused Vitest validation passed for the new route suite. (src/app/api/forms/instances/[id]/route.ts, src/app/api/forms/instances/[id]/__tests__/route.test.ts, docs/DATA_MODEL.md, docs/ENGINEERING_LOG.md)

- 2026-03-08T21:25:00Z — Profile save-consent hardening: authenticated seeker profile writes are now gated by an explicit local cross-device sync preference so the implementation matches the privacy contract. Signed-in users can stay local-only until they opt in, enabling sync now performs an immediate best-effort account save, and city/language/seeker-profile writes no longer call `/api/profile` unless sync is on. Updated seeker profile copy, privacy controls, and regression tests for unauthenticated local-only saves, authenticated local-only state, and explicit opt-in sync. Repo-wide `tsc --noEmit` still fails only in parallel-lane ingestion/demo files. (src/app/(seeker)/profile/ProfilePageClient.tsx, src/app/(seeker)/__tests__/profile-page-client.test.tsx, src/services/profile/__tests__/profile-api.test.ts, docs/SECURITY_PRIVACY.md, src/services/profile/README.md, reports/repo_pipeline_unification_enhancements.md, reports/repo_pipeline_reunification_plan.md, docs/ENGINEERING_LOG.md)

- 2026-03-08T21:05:00Z — Phase 5 completion gate closed: added an ORAN-admin `discovery-preview` surface that consumes the same discovery grammar as Directory, Map, and Chat, plus parity tests that prove selector round-tripping, trust-floor consistency, and deterministic rejection of unsupported attribute selectors. The shared discovery compiler now drops unknown attribute dimensions/tags at the boundary, and ORAN admin navigation exposes the preview as a first-class tool. Validation green for the phase lane: 92 targeted discovery/chat/admin tests passing; repo-wide `tsc --noEmit` remains blocked only by parallel-lane files in `scripts/run-pipeline-demo.ts`, `src/agents/ingestion/__tests__/taxonomyFederation.schema.test.ts`, and `src/agents/ingestion/persistence/storeFactory.ts`. (src/services/search/discovery.ts, src/services/search/__tests__/discovery.test.ts, src/services/search/__tests__/discoveryParity.test.ts, src/app/(oran-admin)/discovery-preview/DiscoveryPreviewPageClient.tsx, src/app/(oran-admin)/discovery-preview/page.tsx, src/app/(oran-admin)/__tests__/discovery-preview-page.test.tsx, src/app/(oran-admin)/OranAdminLayoutShell.tsx, docs/ENGINEERING_LOG.md)

- 2026-03-08T18:40:00Z — Phase 5 seeker entry-link unification: added shared discovery href generation so chat, profile, saved, directory, and map can hand off the same canonical discovery intent instead of resetting users to blank browse pages. Chat now preserves incoming discovery context in its Directory/Map links, Directory/Map now preserve current text/category/trust/sort/taxonomy state in Chat handoffs, and Profile/Saved now use the seeker’s primary saved service-interest to seed Directory/Map/Chat entry links when no richer context exists. Added regression coverage for chat link preservation, saved/profile personalized empty-state links, and cross-surface canonical handoff. Full `tsc --noEmit` still remains blocked outside this slice by `scripts/run-pipeline-demo.ts`, `src/app/(seeker)/service/[id]/ServiceDetailClient.tsx`, and ingestion-lane taxonomy federation test errors. (src/services/search/discovery.ts, src/services/profile/discoveryPreference.ts, src/domain/discoveryNeeds.ts, src/app/(seeker)/chat/ChatPageClient.tsx, src/app/(seeker)/saved/SavedPageClient.tsx, src/app/(seeker)/profile/ProfilePageClient.tsx, src/app/(seeker)/directory/DirectoryPageClient.tsx, src/app/(seeker)/map/MapPageClient.tsx, src/app/(seeker)/__tests__/chat-page-client.test.tsx, src/app/(seeker)/__tests__/saved-page-client.test.tsx, src/app/(seeker)/__tests__/profile-page-client.test.tsx, src/app/(seeker)/__tests__/directory-page-client.test.tsx, src/app/(seeker)/__tests__/map-page-client.test.tsx, src/services/search/__tests__/discovery.test.ts, src/domain/__tests__/discoveryNeeds.test.ts, docs/ENGINEERING_LOG.md)

- 2026-03-08T18:20:00Z — Phase 5 map/directory URL-state unification: map now hydrates from the same canonical discovery URL grammar as directory (`q`, `category`, `confidence`, `sort`, `taxonomyIds`) and normalizes legacy category aliases back to canonical discovery IDs before replaying the search. Map search success now rewrites shareable URLs through the shared serializer while deliberately keeping bbox/device-location state ephemeral for privacy. Cross-surface handoff between directory and map now carries canonical discovery intent instead of dropping users into blank views, and new regression tests lock down canonical map hydration, clear-search URL unwind, and directory-to-map handoff behavior. Full `tsc --noEmit` remains blocked outside this slice by `scripts/run-pipeline-demo.ts` and ingestion-lane taxonomy federation test errors. (src/app/(seeker)/map/MapPageClient.tsx, src/app/(seeker)/directory/DirectoryPageClient.tsx, src/app/(seeker)/__tests__/map-page-client.test.tsx, src/app/(seeker)/__tests__/directory-page-client.test.tsx, docs/ENGINEERING_LOG.md)

- 2026-03-08T17:40:00Z — Phase 5 discovery-query unification pass: tightened seeker discovery behavior so category-clearing and clear-search actions preserve active taxonomy/attribute/location filters instead of dropping back to an empty state, and fixed map taxonomy/category interactions so quick chips remain in sync with actual search intent. Introduced a shared discovery query compiler/serializer for directory, map, and chat (`buildSearchQueryFromDiscovery`, API param serialization, URL param serialization, shared confidence/sort labels), then rewired the seeker surfaces and chat retrieval to use it. Added focused search compiler coverage plus new directory/map regression tests for category-only, filter-preserving, and stale-query edge cases. Full `tsc --noEmit` is still blocked by pre-existing errors in `scripts/run-pipeline-demo.ts` and new ingestion-lane taxonomy federation tests outside this slice. (src/services/search/discovery.ts, src/services/search/__tests__/discovery.test.ts, src/app/(seeker)/directory/DirectoryPageClient.tsx, src/app/(seeker)/map/MapPageClient.tsx, src/services/chat/retrievalProfile.ts, src/app/(seeker)/__tests__/directory-page-client.test.tsx, src/app/(seeker)/__tests__/map-page-client.test.tsx, docs/ENGINEERING_LOG.md)

- 2026-03-08T17:15:00Z — Phase 5 unification slice: introduced a shared discovery-need registry so seeker service-interest IDs, chat intent categories, retrieval query hints, and directory/map quick-category chips all draw from one source of truth. Directory category links now normalize legacy values (for example `category=food`) into canonical need IDs while still searching with human-readable query text, and category-only directory links now auto-run correctly without requiring duplicate `q` params. Added focused registry tests plus seeker directory/map/profile and chat schema/route validation. Full `tsc --noEmit` still remains blocked by pre-existing syntax errors in `src/agents/ingestion/entityResolution.ts`, outside this slice. (src/domain/discoveryNeeds.ts, src/domain/__tests__/discoveryNeeds.test.ts, src/services/profile/contracts.ts, src/services/chat/types.ts, src/services/chat/retrievalProfile.ts, src/app/(seeker)/profile/ProfilePageClient.tsx, src/app/(seeker)/directory/DirectoryPageClient.tsx, src/app/(seeker)/map/MapPageClient.tsx, src/app/(seeker)/__tests__/directory-page-client.test.tsx, docs/ENGINEERING_LOG.md)

- 2026-03-08T12:45:00Z — Documentation reconciliation pass: updated `docs/platform/INTEGRATIONS.md` to reflect the current Azure integration surface (hybrid DB-backed flags, OpenAI, Redis, ACS email, Functions, Speech, and SessionProvider wiring), extended `docs/DATA_MODEL.md` with the 0032 source assertion layer and 0033 canonical federation layer, corrected inconsistent status reporting in `docs/foundry_integrations.md`, and marked `docs/audit/AUDIT_REPORT.md` as a historical baseline with a current-state warning. (docs/platform/INTEGRATIONS.md, docs/DATA_MODEL.md, docs/foundry_integrations.md, docs/audit/AUDIT_REPORT.md, docs/ENGINEERING_LOG.md)

- 2026-03-08T00:00:00Z — Chat Phase 1 seeker constraints: added first-class authenticated seeker fields for transportation barriers, preferred delivery modes, same-day / next-day urgency, documentation barriers, and digital-access barriers; persisted them in `seeker_profiles`, surfaced them in the seeker profile UI, hydrated them into authenticated chat context, and mapped them into deterministic `access`, `delivery`, and `situation` profile signals without introducing any LLM retrieval logic. Updated tests and SSOT docs to reflect the new contract and deliberate non-mappings. (src/services/profile/contracts.ts, src/app/api/profile/route.ts, src/services/profile/chatHydration.ts, src/services/chat/types.ts, src/services/chat/retrievalProfile.ts, src/app/(seeker)/profile/ProfilePageClient.tsx, src/services/profile/__tests__/profile-api.test.ts, src/services/profile/__tests__/chatHydration.test.ts, src/services/chat/__tests__/retrieval-profile.test.ts, src/app/api/chat/__tests__/route.test.ts, src/app/(seeker)/__tests__/profile-page-client.test.tsx, src/db/schema.ts, db/migrations/0036_seeker_profile_phase1_constraints.sql, docs/CHAT_ARCHITECTURE.md, docs/DATA_MODEL.md, src/services/chat/README.md, src/services/profile/README.md, docs/ENGINEERING_LOG.md)

- 2026-03-08T00:00:00Z — Published listing lifecycle hardening for stale and flagged resources: confidence regression detection now emits explicit `reverify` vs `suppress` actions, folds `community_report` submissions into feedback severity thresholds, and suppresses seeker-visible listings by setting `services.status = 'inactive'` when a live record is critically degraded, severely stale, or repeatedly flagged. The internal scan endpoint and timer function now report `suppressedCount`, and new regression policy tests lock the behavior down. (src/services/regression/detector.ts, src/services/regression/policy.ts, src/services/regression/__tests__/detector.test.ts, src/services/regression/__tests__/policy.test.ts, src/app/api/internal/confidence-regression-scan/route.ts, src/app/api/internal/confidence-regression-scan/__tests__/route.test.ts, functions/scanConfidenceRegressions/index.ts, docs/contracts/INGESTION_CONTRACT.md, docs/agents/AGENTS_INGESTION_PIPELINE.md, docs/ENGINEERING_LOG.md)
- 2026-03-07T18:00:42Z — Chat hydration hardening pass after end-to-end audit: (1) Request-time locale is now authoritative for live chat turns; server hydration no longer overwrites locale mid-request. (2) `serviceInterests` are now validated against a closed seeker-profile vocabulary, and chat retrieval only appends normalized query hints for recognized IDs. (3) Language-support profile shaping was corrected to match canonical taxonomy dimensions: `interpreter_on_site` remains an `access` tag while `bilingual_services` is applied only under `culture`. (4) Personalized chat retrieval now opts out of shared Redis search caching to align runtime behavior with the documented search-cache contract. (5) Added regression tests for taxonomy-correct profile signals, normalized service-interest shaping, request-locale authority during hydration, and personalized cache bypass. (src/services/profile/contracts.ts, src/services/profile/chatHydration.ts, src/services/chat/retrievalProfile.ts, src/services/search/types.ts, src/services/search/cache.ts, src/services/chat/__tests__/retrieval-profile.test.ts, src/services/profile/__tests__/chatHydration.test.ts, src/services/search/__tests__/cache.test.ts, src/app/api/chat/__tests__/route.test.ts, docs/CHAT_ARCHITECTURE.md, docs/SCORING_MODEL.md, docs/DECISIONS/ADR-0008-chat-profile-hydration-and-deterministic-retrieval-shaping.md, docs/ENGINEERING_LOG.md)

- 2026-03-07T06:34:10Z — Chat profile hydration completed as a server-side, contract-backed feature: (1) Added `hydrateChatContext()` to load authenticated chat context from `user_profiles` + `seeker_profiles` with fail-open behavior. (2) Added deterministic chat retrieval shaping helpers that convert exact seeker profile fields into `cityBias` plus schema-backed `profileSignals` for `population`, `situation`, `access`, `delivery`, and `culture` taxonomies only. (3) Chat retrieval now uses hydrated context and appends saved service-interest terms only for `general` intents. (4) Search engine now computes `profile_match_score` as a secondary ordering hint after verification confidence, with no new records introduced and no LLM involvement. (5) Added targeted chat/search tests plus SSOT/ADR documentation updates describing supported mappings and deliberate non-mappings. (src/services/profile/chatHydration.ts, src/services/chat/retrievalProfile.ts, src/services/chat/orchestrator.ts, src/app/api/chat/route.ts, src/services/search/types.ts, src/services/search/engine.ts, src/services/chat/__tests__/intent-schema.test.ts, src/app/api/chat/__tests__/route.test.ts, src/services/search/__tests__/query-builder.test.ts, docs/CHAT_ARCHITECTURE.md, docs/SCORING_MODEL.md, docs/DECISIONS/ADR-0008-chat-profile-hydration-and-deterministic-retrieval-shaping.md, docs/ENGINEERING_LOG.md)

- 2026-03-12T06:00:00Z — HSDS/211 Phase 2 Post-Implementation Audit: Cross-cutting audit of all Phase 1+2 ingestion work (2 migrations, 13 schema tables, 11 store interfaces, 11 implementations, 29 test files). Found 9 findings (4M, 5L). Fixes applied: (M1) Added missing `listByLifecycle` to CanonicalServiceStore interface + implementation — SQL index existed but store method was absent. (M2) Added missing `listByLifecycle`, `listByPublication`, `updatePublicationStatus` to CanonicalLocationStore interface + implementation — 3 methods required for parity with CanonicalOrganizationStore. (M3) Added `source_count INT NOT NULL DEFAULT 1` to `canonical_locations` in migration SQL + Drizzle schema — was present on orgs/services but missing on locations. (M4) Fixed all 5 canonical test makeRow() fixtures — field names didn't match Drizzle schema (e.g. `address1`→`addressLine1`, `fundingFramework`→removed, `sourceSystemId`→removed, `locationType`→removed). Added 4 new tests: `listByLifecycle` (service), `listByLifecycle`/`listByPublication`/`updatePublicationStatus` (location). LOW findings accepted: (L1) redundant `updatedAt` in stores (trigger handles it), (L2) no store methods for winning_source_system_id queries (Phase 4+), (L3) supersedeField needs transaction wrapping (Phase 4), (L4) entity_identifiers reuse documentation imprecise, (L5) unused type imports acceptable. TypeScript 0 errors, 143/143 persistence tests pass (up from 139). (src/agents/ingestion/stores.ts, src/agents/ingestion/persistence/canonicalServiceStore.ts, src/agents/ingestion/persistence/canonicalLocationStore.ts, db/migrations/0033_canonical_federation_layer.sql, src/db/schema.ts, src/agents/ingestion/persistence/__tests__/canonical*.test.ts, docs/ENGINEERING_LOG.md)

- 2026-03-12T00:00:00Z — HSDS/211 Phase 2: Canonical Federation Layer: (1) Migration `0033_canonical_federation_layer.sql` — 5 new tables (`canonical_organizations`, `canonical_services`, `canonical_locations`, `canonical_service_locations`, `canonical_provenance`). CHECK constraints on lifecycle_status, publication_status, canonical_entity_type, decision_status, confidence_hint. FK cascades: parent→child CASCADE, optional refs SET NULL. PostGIS `sync_canonical_location_geom()` function + idempotent trigger. GiST spatial, GIN text-search, partial indexes on decision_status. `updated_at` triggers on all 5 tables. (2) Drizzle schema extended with 5 table definitions, 10 type exports, 5 relation definitions. (3) 5 store interfaces added to `stores.ts` + `IngestionStores` composite updated (now 27 stores). (4) 5 Drizzle persistence implementations. (5) `storeFactory.ts` and `persistence/index.ts` updated. (6) 33 new unit tests. (7) Inline audit: 3 medium findings fixed (non-idempotent geom trigger, missing reverse-lookup index, missing publication_status index). 2 low findings accepted. (8) Design decision: reuses Phase 1 `entity_identifiers` instead of new `canonical_identifiers` table. TypeScript 0 errors, 2403/2438 tests pass (35 pre-existing failures). (db/migrations/0033_canonical_federation_layer.sql, src/db/schema.ts, src/agents/ingestion/stores.ts, src/agents/ingestion/persistence/canonical*.ts, src/agents/ingestion/persistence/__tests__/canonical*.test.ts, src/agents/ingestion/persistence/storeFactory.ts, src/agents/ingestion/persistence/index.ts, hsds_211_integration_plan.md, docs/ENGINEERING_LOG.md)

- 2026-03-07T06:15:00Z — Seeker profile persistence + shared portal account controls: (1) Added shared authenticated header account control `PortalUserMenu` and wired sign-out into ORAN admin, host, and community-admin layouts. (2) Improved seeker profile UX copy/trust language and surfaced sign-out/export actions more clearly. (3) Added shared seeker-profile contract (`src/services/profile/contracts.ts`) and migration `0034_seeker_profiles.sql` for authenticated seeker-specific profile context separate from shared `user_profiles`; later extended it with accessibility, pronouns, profile headline, avatar emoji, accent theme, and account-detail sync support before migration apply. (4) Extended `GET/PUT /api/profile` to return and persist `seekerProfile` fields plus account identity fields, added password update route for credentials users, updated delete/export privacy routes to include `seeker_profiles`, and fixed `saved_services.saved_at` export selection. (5) Updated domain/schema/docs/tests for the richer profile contract. (src/components/ui/portal-user-menu.tsx, src/app/(oran-admin)/OranAdminLayoutShell.tsx, src/app/(host)/HostLayoutShell.tsx, src/app/(community-admin)/CommunityAdminLayoutShell.tsx, src/app/(seeker)/profile/ProfilePageClient.tsx, src/app/(seeker)/__tests__/profile-page-client.test.tsx, src/services/profile/contracts.ts, src/app/api/profile/route.ts, src/app/api/user/security/password/route.ts, src/app/api/user/data-delete/route.ts, src/app/api/user/data-export/route.ts, src/services/profile/__tests__/profile-api.test.ts, src/domain/types.ts, src/db/schema.ts, db/migrations/0034_seeker_profiles.sql, docs/DATA_MODEL.md, docs/ENGINEERING_LOG.md)

- 2026-03-08T00:00:00Z — HSDS/211 Phase 1 Audit: 13 findings across 4 severity tiers (C2, H5, M4, L2). Fixes applied: (C1) hsdsExportSnapshotStore.create() now withdraws existing 'current' snapshots before insert to prevent unique partial index violation. (C2) sourceRecordStore.bulkCreate() uses onConflictDoNothing for idempotent dedup. (H1) Mid-file imports in stores.ts moved to top. (H2) listPending FIFO ordering via asc(createdAt). (H3/H4) Unused imports removed from sourceFeedStore/entityIdentifierStore. (M3) sourceSystemStore.update() omits id/createdAt from spread. (M4) sourceFeedStore.updateAfterPoll resets errorCount on success. (L2) listByFeed method added to SourceRecordStore interface + implementation. (L1) 36 unit tests added across 6 new test files. M1/M2 (Drizzle partial index drift) accepted — SQL migrations are DDL source of truth. TypeScript 0 errors, 2400/2406 tests pass (6 pre-existing UI failures). (src/agents/ingestion/stores.ts, src/agents/ingestion/persistence/sourceSystemStore.ts, src/agents/ingestion/persistence/sourceFeedStore.ts, src/agents/ingestion/persistence/sourceRecordStore.ts, src/agents/ingestion/persistence/entityIdentifierStore.ts, src/agents/ingestion/persistence/hsdsExportSnapshotStore.ts, src/agents/ingestion/persistence/__tests__/sourceSystemStore.test.ts, src/agents/ingestion/persistence/__tests__/sourceFeedStore.test.ts, src/agents/ingestion/persistence/__tests__/sourceRecordStore.test.ts, src/agents/ingestion/persistence/__tests__/entityIdentifierStore.test.ts, src/agents/ingestion/persistence/__tests__/hsdsExportSnapshotStore.test.ts, src/agents/ingestion/persistence/__tests__/lifecycleEventStore.test.ts, docs/ENGINEERING_LOG.md)

- 2026-03-07T00:00:00Z — HSDS/211 Phase 1: Source Assertion Layer: (1) Migration `0032_source_assertion_layer.sql` — 7 new tables (`source_systems`, `source_feeds`, `source_records`, `source_record_taxonomy`, `entity_identifiers`, `hsds_export_snapshots`, `lifecycle_events`). Data migration from `ingestion_sources` → `source_systems` with trust-tier mapping; default `source_feeds` per system; `source_system_id` FK added to `ingestion_jobs` with backfill. (2) Drizzle schema extended in `src/db/schema.ts` with 7 table defs, type exports, relations, and `sourceSystemId` FK on `ingestionJobs`. (3) `contracts.ts` expanded: `SourceSystemFamilySchema`, `TrustTierSchema`, 5 new `SourceKindSchema` values. (4) 6 store interfaces added to `stores.ts` (`SourceSystemStore`, `SourceFeedStore`, `SourceRecordStore`, `EntityIdentifierStore`, `HsdsExportSnapshotStore`, `LifecycleEventStore`) plus `IngestionStores` composite updated. (5) 6 Drizzle persistence implementations created under `persistence/`. (6) `storeFactory.ts` and `persistence/index.ts` updated. (7) Planning doc `hsds_211_integration_plan.md` status updated to Phase 1 complete. (db/migrations/0032_source_assertion_layer.sql, src/db/schema.ts, src/agents/ingestion/contracts.ts, src/agents/ingestion/stores.ts, src/agents/ingestion/persistence/sourceSystemStore.ts, src/agents/ingestion/persistence/sourceFeedStore.ts, src/agents/ingestion/persistence/sourceRecordStore.ts, src/agents/ingestion/persistence/entityIdentifierStore.ts, src/agents/ingestion/persistence/hsdsExportSnapshotStore.ts, src/agents/ingestion/persistence/lifecycleEventStore.ts, src/agents/ingestion/persistence/storeFactory.ts, src/agents/ingestion/persistence/index.ts, hsds_211_integration_plan.md, docs/ENGINEERING_LOG.md)

- 2026-03-06T14:00:00Z — Phase 8 (i18n) + design-token audit improvements: (1) AUDIT: Extended semantic colour token set in `globals.css` `@theme inline` by 9 new tokens (`--color-error-soft/accent/pale/light/deep`, `--color-action-soft/pale/mid/max`) covering red-200/300/400/500/800 and blue-200/300/400/900 scales. (2) `button.tsx` `crisis` hover fixed: `hover:bg-red-800` → `hover:bg-error-deep`. (3) Global sed pass 2 across 35 `src/app/**/*.tsx` files — tokenised `text-red-*`, `text-blue-*`, `border-red-*`, `border-blue-*`, `ring-blue-*`, `ring-red-*` and `outline-blue-*` (143+ individual class occurrences). (4) `not-found.tsx` stale `outline-blue-600` replaced by `outline-action-base`. (5) `scripts/audit-ui-consistency.mjs` upgraded: `detectRawButtons` now reports raw `<button>` alongside Button import as advisory (not silent pass); `detectAdHocColors` regex expanded from `bg-*`-only to `bg|text|border|outline|ring` across both red and blue scales; `sep` import moved to top; remediation table updated. (6) PHASE 8 i18n: created `src/locales/{en,es,zh,ar,vi,fr}.json` — en.json is canonical; non-en files are English-fallback placeholders ready for translator hand-off. (7) `src/services/i18n/i18n.ts` updated: inline `en` dictionary removed; all 6 locale JSONs statically imported and pre-populated into `localeCache` at module load. (8) `src/lib/locale.ts` created: `resolveLocale()` async Server Component helper — reads `NEXT_LOCALE` cookie → `Accept-Language` header → `DEFAULT_LOCALE`. (9) `src/app/layout.tsx` converted to async Server Component; calls `resolveLocale()` + `isRTL()` to set `<html lang={locale} dir={dir}>` at render time. (10) `docs/solutions/I18N_WORKFLOW.md` updated: Implementation Status block rewritten; `<html lang>` policy section updated to reflect Phase 8 resolution. (11) `docs/agents/ROADMAP.md` — Phase 8 added (✅). (12) `docs/ui/UI_SURFACE_MAP.md` — `/claim` updated 🔲→✅. (src/app/globals.css, src/components/ui/button.tsx, src/app/not-found.tsx, scripts/audit-ui-consistency.mjs, src/locales/*.json, src/services/i18n/i18n.ts, src/lib/locale.ts, src/app/layout.tsx, docs/solutions/I18N_WORKFLOW.md, docs/agents/ROADMAP.md, docs/ui/UI_SURFACE_MAP.md)
- 2026-03-06T10:00:00Z — UI drift fixes + doc sync: (1) Added 8 semantic colour tokens to `globals.css` `@theme inline` block (`--color-error-subtle/muted/strong`, `--color-info-subtle/muted`, `--color-action/action-base/action-strong`). (2) Global sed replacement across all 35 flagged `src/app/**/*.tsx` files — raw `bg-blue-*`/`bg-red-*` utilities replaced with semantic token classes. (3) `button.tsx` `default` and `crisis` variants updated to `bg-action-base`/`bg-action-strong` and `bg-error-strong` respectively. (4) Replaced raw `<button onClick={reset}>` with `<Button>` component in 5 error boundary pages (`error.tsx`, `(seeker)/error.tsx`, `(host)/error.tsx`, `(community-admin)/error.tsx`, `(oran-admin)/error.tsx`). (5) `scripts/audit-ui-consistency.mjs` updated to exclude `global-error.tsx` (inline-styles-only by design) and `__tests__/**` dirs. (6) `docs/agents/ROADMAP.md` — rewritten to reflect all Phases 1–6 as ✅ complete; Phase 7 (Deployment & Ops) added as next priority. (7) `docs/agents/AGENTS_OVERVIEW.md` — added full built inventory (9-stage pipeline, 16 stores, 7 Azure Functions, 19 test files, 27 migrations, module table). (8) `docs/ui/UI_SURFACE_MAP.md` — updated 4 oran-admin routes from 🔲 to ✅; added `/ingestion`, `/scopes` (oran-admin); added `/appeal`, `/notifications`, `/report` (seeker). (9) `docs/governance/GOVERNANCE.md` — added Ingestion & Approval Workflow section (pipeline gate, two-person scope grant rule, SLA tiers, escalation chain). (10) `docs/governance/ROLES_PERMISSIONS.md` — added 7 ingestion-specific permission rows (`ingestion_sources`, `ingestion_jobs`, `ingestion_candidates`, `candidate_assignments`, `tag_confirmations`, `source_registry`, `evidence_snapshots`).

- 2026-03-11T00:00:00Z — Phase 2 Audit + Phase 3 Implementation Complete: (1) PHASE 2 AUDIT: Reviewed all Phase 1+2 code. Found and fixed 4 issues: (a) BUG — `adminProfileStore.update()` didn't map `maxInReview` to DB column `max_in_review`; (b) IMPROVEMENT — attribute filter validation in search route now enforces key≤50 and tag≤100 length limits; (c) OPTIMIZATION — `ensureDefaultNotificationPreferences()` batched from 6 sequential INSERTs to 1 multi-row INSERT; (d) CLEANUP — `computeAdminPriority()` returns `matchLevel: 'none'` instead of `'fallback'` with `score: 0`. 2185 tests passing after audit fixes. (2) GAP M2 — Composite search presets: 8 presets (low_cost_dental, free_food, free_medical, walk_in_mental_health, virtual_counseling, housing_assistance, legal_aid_free, veteran_services) in `presets.ts`; `getSearchPreset(id)`, `mergePresetFilters()` with user-override semantics; wired into `GET /api/search` route via `preset` query param. 17 new tests. (3) GAP M4 — Auto-capacity scaling: `computeEffectiveMaxPending()` with 3 tiers (fast <4h → 1.5x, normal 4-12h → 1.0x, slow >12h → 0.75x), minimum 20 completed reviews before scaling applies, floor of 1. 7 new tests. (4) GAP M5 — Organization service caps: `organization_settings` table with `max_services` column (default 100, range 1-10000); `getOrgServiceCapStatus()`, `canOrgAddService()`, `setOrgMaxServices()` with upsert. 12 new tests. (5) Geographic coverage gap alerting: `alertCoverageGaps` Azure Function (daily 8 AM UTC timer); `src/services/coverage/gaps.ts` with `findUnroutedCandidates()`, `getCoverageGapSummaries()`, `getCoverageGapReport()`, `alertOranAdminsAboutGaps()` (date-based idempotency keys, ON CONFLICT DO NOTHING); `POST /api/internal/coverage-gaps` route with Bearer auth and Zod validation. 25 new tests. (6) Admin capacity dashboard: `GET /api/admin/capacity` with `computeEffectiveMaxPending()` integration, returns scaling-aware `effectiveMaxPending` and `scalingApplied` flag. 8 new tests. Total: 2283 tests passing (98 new), 0 new TSC errors. (src/services/search/presets.ts, src/services/search/__tests__/presets.test.ts, src/app/api/search/route.ts, src/app/api/search/__tests__/route.test.ts, src/agents/ingestion/routing.ts, src/agents/ingestion/__tests__/routing.test.ts, src/agents/ingestion/persistence/adminProfileStore.ts, src/services/escalation/engine.ts, src/services/organizations/serviceCaps.ts, src/services/organizations/__tests__/serviceCaps.test.ts, src/services/coverage/gaps.ts, src/services/coverage/__tests__/gaps.test.ts, src/app/api/internal/coverage-gaps/route.ts, src/app/api/internal/coverage-gaps/__tests__/route.test.ts, src/app/api/admin/capacity/route.ts, src/app/api/admin/capacity/__tests__/route.test.ts, functions/alertCoverageGaps/index.ts, functions/alertCoverageGaps/function.json, functions/alertCoverageGaps/__tests__/index.test.ts, docs/solutions/IMPLEMENTATION_PLAN.md, src/app/api/README.md, docs/ENGINEERING_LOG.md)

- 2026-03-10T00:00:00Z — Phase 1 Audit + Phase 2 Implementation Complete: (1) PHASE 1 AUDIT: Self-reviewed all Phase 1 code (~1500 lines). Found and fixed 4 bugs: test count hardcoding (DEFAULT_ADMIN_NOTIFICATION_EVENTS grew from 4→6 but tests used literal 4), race condition in `escalateToOranAdmin` (from_status queried AFTER update changed it), orphan escalation (status transitioned even with no ORAN admins), and fragile mock sequences. 63/63 Phase 1 tests passing. (2) GAP C3 — Service attribute search filters: Added `attributeFilters` field to `SearchFiltersSchema` (Record<string, string[]>), added per-taxonomy EXISTS subquery SQL generation to `buildFiltersWhereClause()`, added `attributes` JSON query param to `GET /api/search` with Zod validation. 8 new tests, 33/33 search tests. (3) GAP H5 — Submitter routing notification: Added optional `submittedByUserId` to `RouteQueueMessage`, fires `submission_status_changed` notification to submitter in fallback routing block. 7 new tests for routeToAdmin. (4) GAP M1 — Scope-differentiated capacity: Added `ROLE_CAPACITY_DEFAULTS` constant (oran_admin: 50/20, community_admin: 10/5, host_admin: 5/3), updated `createAdminProfile` factory and `AdminProfileSchema` with `maxInReview` field, wired role-based defaults through store. 2 new tests, 87/87 admin tests. (5) GAP M3 — Auto-pause at capacity: Added `shouldToggleAcceptingNew()` to routing.ts with AUTO_RESUME_THRESHOLD=0.8, added `setAcceptingNew` to AdminCapacityStore interface. 6 new tests, 34/34 routing tests. (6) GAP H3 — Service type faceted filter UI: Added collapsible attribute dimension filter section to DirectoryPageClient.tsx with delivery/cost/access common tag chips, `selectedAttributes` state seeded from URL, wired to `?attributes=` query param, integrated into pushUrlState/clearAllFilters/Applied bar. (src/services/escalation/engine.ts, src/services/escalation/__tests__/engine.test.ts, src/services/search/types.ts, src/services/search/engine.ts, src/app/api/search/route.ts, src/services/search/__tests__/query-builder.test.ts, src/app/api/search/__tests__/route.test.ts, functions/routeToAdmin/index.ts, functions/routeToAdmin/__tests__/index.test.ts, vitest.config.ts, src/domain/constants.ts, src/agents/ingestion/adminProfiles.ts, src/agents/ingestion/routing.ts, src/agents/ingestion/persistence/adminProfileStore.ts, src/agents/ingestion/__tests__/adminProfiles.test.ts, src/agents/ingestion/__tests__/admin-approval.test.ts, src/agents/ingestion/__tests__/routing.test.ts, src/app/(seeker)/directory/DirectoryPageClient.tsx, docs/solutions/IMPLEMENTATION_PLAN.md, docs/ENGINEERING_LOG.md)

- 2026-03-05T23:00:00Z — Agent SKY Activation — TASK-13 through TASK-17 complete: (1) TASK-13: Created `eslint-plugin-oran.mjs` (local inline ESLint plugin — `eslint-plugin-tailwindcss` incompatible with Tailwind v4 `resolveConfig` export); rule `oran/no-unapproved-arbitrary` warns on unapproved Tailwind arbitrary values; approved set covers CSS vars, touch targets, grids, positioning, and animation delays; §11 added to `docs/ui/UI_UX_TOKENS.md`. Fixed 17 pre-existing lint errors (unused imports in 5 test files, `mockTransaction`, `sortPart`, `init` param, `useCallback`-after-return in `MapContainer.tsx`, `setState-in-effect` in `CommandPalette.tsx`). (2) TASK-14: Added reduced-motion CSS var layer (`--transition-fast: 150ms ease`, `--transition-standard: 250ms ease`, `--transition-slow: 400ms ease` in `globals.css`; `@media (prefers-reduced-motion: reduce)` zeros all three); `card-enter` and `page-enter` keyframes use the vars so reduced-motion users get instant transitions without extra media queries on each consumer; `Button` base class changed from `transition-colors` to `transition-[var(--transition-fast)]`. (3) TASK-15: Created `scripts/audit-ui-consistency.mjs` — scans `src/app/**/*.tsx` for raw `<button` without shared import, ad-hoc `bg-blue-*`/`bg-red-*` colour drift, and `<input` without label association; non-blocking CI job appends output to `$GITHUB_STEP_SUMMARY`. (4) TASK-16: Full accessibility audit of all 12 admin surfaces (oran-admin, community-admin, host portals) — all pass h1/label/th-scope/aria-label/main-landmark/Dialog checklist. (5) TASK-17: Removed `updateHtmlLang()` from `ProfilePageClient.tsx` (Option C) — `lang="es"` with English text applies wrong TTS engine and harms screen readers; `<html lang>` must only change when translated string bundles are served; decision recorded in `docs/solutions/I18N_WORKFLOW.md`. 0 TypeScript errors, 0 ESLint errors post-sprint. (`eslint-plugin-oran.mjs`, `eslint.config.mjs`, `src/app/globals.css`, `src/components/ui/button.tsx`, `src/app/(seeker)/layout.tsx`, `src/components/directory/ServiceCard.tsx`, `src/app/(seeker)/profile/ProfilePageClient.tsx`, `scripts/audit-ui-consistency.mjs`, `package.json`, `.github/workflows/ci.yml`, `docs/ui/UI_UX_TOKENS.md`, `docs/solutions/I18N_WORKFLOW.md`, `docs/agents/status/STATUS_OMEGA.md`)

- 2026-03-09T22:00:00Z — Idea 16 — App Insights AI Observability: Added `trackAiEvent(name, payload)` to `src/services/telemetry/appInsights.ts`. The helper auto-splits mixed payloads into App Insights `properties` (strings/booleans) and `measurements` (numbers); fail-open (swallows all telemetry errors). Instrumented all live AI call sites: `llm_summarize` (chat summarizer — duration, tokens, model, service count), `content_safety_check` (crisis gate — duration, severity score, triggered flag), `embedding_call` (Cohere embed — duration, input_type, model), `ingestion_llm_extract` + `ingestion_llm_categorize` (pipeline stages — duration, model, confidence/category count), `review_assist` (admin LLM review — duration, completeness score, warning count, model), `feedback_triage` (gpt-4o-mini triage — duration, category, urgency). No PII emitted in any payload. 2,032 tests passing, 0 TypeScript errors. (`src/services/telemetry/appInsights.ts`, `src/services/chat/llm.ts`, `src/services/security/contentSafety.ts`, `src/services/search/embeddings.ts`, `src/agents/ingestion/pipeline/stages.ts`, `src/services/admin/reviewAssist.ts`, `src/services/feedback/triage.ts`, `docs/foundry_integrations.md`)

- 2026-03-09T12:00:00Z — Azure provisioning complete (Phase 3+4 env vars): Created 7 missing Key Vault secrets (`azure-speech-key`, `azure-speech-region`, `azure-translator-endpoint`, `azure-translator-region`, `foundry-endpoint`, `foundry-key`, `foundry-embed-deployment`) in `oranhf57ir-prod-kv`. Added 6 App Service appsettings to `oranhf57ir-prod-web` (`AZURE_SPEECH_KEY`, `AZURE_SPEECH_REGION`, `FOUNDRY_ENDPOINT`, `FOUNDRY_KEY`, `FOUNDRY_EMBED_DEPLOYMENT`, `FOUNDRY_API_VERSION` — secrets via KV VaultName references). Added 4 Function App settings to `oranhf57ir-prod-func` (`FOUNDRY_ENDPOINT`, `FOUNDRY_KEY` as KV refs; `FOUNDRY_EXTRACT_DEPLOYMENT=phi-4-mini-instruct`, `FOUNDRY_API_VERSION=2024-08-01-preview` as plain values). Total KV secrets: 19. All Phase 1–4 Foundry integrations are fully provisioned.

- 2026-03-06T12:00:00Z — Phase 4 (Seeker UX & Accessibility) + Phase 3 audit fixes: (1) AUDIT FIXES: `src/services/search/embeddings.ts` — critical bug: removed non-existent `eligibility` column from `getServicesNeedingEmbedding` SQL; removed unsupported `dimensions` param from Cohere API call; added `AbortSignal.timeout(8_000)`. `functions/verifyCandidate/index.ts` — corrected penalty comment. `src/services/search/engine.ts` — replaced `eslint-disable-line` hack with typed `stripHelpers` fn. (2) FEATURE_FLAGS: added `LLM_INTENT_ENRICH`, `MULTILINGUAL_DESCRIPTIONS`, `TTS_SUMMARIES`; all disabled-by-default. (3) Idea 10: `src/services/chat/intentEnrich.ts` — gpt-4o-mini 'general' query classifier; fail-open; crisis-safe. `OrchestratorDeps.enrichIntent?` added; Stage 4.5 wired (only for 'general' + flag). (4) Idea 8: `translateBatch` wired in `POST /api/chat` post-assembly; gated by `multilingual_descriptions` flag + `isTranslatorConfigured()`; fail-open. (5) Idea 9: `geocode()` called in `POST /api/admin/ingestion/candidates/[id]/publish` after submissions bridge; result stored in `extracted_candidates.investigation_pack` JSONB; fail-open. (6) Idea 15: `src/services/tts/azureSpeech.ts` — Azure Speech REST TTS, locale-to-voice map (en/es/zh/ar/vi/fr), 2000-char cap, fail-open. `POST /api/tts/summary` — authenticated, rate-limited (20/min), `tts_summaries` flag gated, returns `audio/mpeg`. (7) `docs/foundry_integrations.md` — Ideas 5, 6, 8, 9, 10, 12, 15 updated to ✅ Live; Phase 4 priority list marked COMPLETE. (src/domain/constants.ts, src/services/flags/flags.ts, src/services/chat/intentEnrich.ts, src/services/chat/orchestrator.ts, src/app/api/chat/route.ts, src/services/tts/azureSpeech.ts, src/app/api/tts/summary/route.ts, src/app/api/admin/ingestion/candidates/[id]/publish/route.ts, src/services/search/embeddings.ts, src/services/search/engine.ts, functions/verifyCandidate/index.ts, docs/foundry_integrations.md)

- 2026-03-06T00:00:00Z — Phase 3 (pgvector + hybrid search): (1) DB migration `0026_pgvector_embeddings.sql` — `CREATE EXTENSION vector`, `embedding vector(1024)` column on services, HNSW index `idx_services_embedding_hnsw` (cosine ops, m=16, ef=64). (2) `FEATURE_FLAGS.VECTOR_SEARCH = 'vector_search'` added to constants; disabled flag in DEFAULT_FLAGS. (3) `src/services/search/embeddings.ts` — Cohere embed service: `embedForQuery`, `embedForIndexing`, `updateServiceEmbedding`, `getServicesNeedingEmbedding`, `buildServiceEmbeddingText`. (4) `src/services/search/vectorSearch.ts` — HNSW query helpers `buildVectorSimilarityQuery`/`buildVectorTopKQuery`, hybrid re-ranker `reRankWithVectorSimilarity` (alpha=0.6 blend). (5) `engine.hybridSearch()` added to `ServiceSearchEngine`. (6) Chat route wired: `vector_search` flag gate → `embedForQuery` → `engine.hybridSearch`. (7) `POST /api/admin/embeddings/reindex` — batch back-fill up to 500 services per call. (8) `POST /api/admin/embeddings/dedup` — HNSW probe per service, returns similarity clusters ≥ 0.92 for admin merge review. (9) `functions/verifyCandidate/index.ts` — Idea 12 cosine calibration: after Phi-4 check, embeds candidate text + page text, applies up to 9-pt penalty for `sim < 0.7`. (db/migrations/0026_pgvector_embeddings.sql, src/domain/constants.ts, src/services/flags/flags.ts, src/services/search/embeddings.ts, src/services/search/vectorSearch.ts, src/services/search/engine.ts, src/app/api/chat/route.ts, src/app/api/admin/embeddings/reindex/route.ts, src/app/api/admin/embeddings/dedup/route.ts, functions/verifyCandidate/index.ts)

- 2026-03-05T07:40:00Z — Confidence regression alerts (first slice): added `confidence_regression` as a universal `submissions.submission_type`, added internal scan endpoint `/api/internal/confidence-regression-scan` (Bearer `INTERNAL_API_KEY`) that creates deduped regression submissions when a verified service changes after its last confidence computation, and added a matching Functions timer stub (`scanConfidenceRegressions`) to call it. (db/migrations/0025_confidence_regression_submissions.sql, src/domain/constants.ts, src/domain/types.ts, src/app/api/internal/confidence-regression-scan/route.ts, functions/scanConfidenceRegressions/*, src/app/api/README.md)

- 2026-03-09T03:12:00Z — Test suite maintenance pass: Fixed 10 failing tests across 4 test files caused by component/route evolution outpacing test mocks. (1) `appeal-page-client.test.tsx` — added `useRouter` mock to `next/navigation`, added `/api/submissions/denied` fetch mock for `fetchDeniedSubmissions`, fixed UUID validation (changed `sub-123` to valid UUID), updated `nthCalledWith` indices for correct call ordering, revised "hides form" test to "disables submission" to match actual component behavior. (2) `report-page-client.test.tsx` — added initial GET `/api/submissions/report` mock for `fetchMyReports` on mount so POST mocks aren't consumed by the initial fetch. (3) `routes.test.ts` (admin) — added `acquireLock` mock returning `true` before `advance` call in org claim approval test. (4) Added 17 new unit tests for shared modules created in prior session: `format.test.ts` (8 tests), `status-styles.test.ts` (4 tests), `status-badge.test.tsx` (5 tests). Final: 168 test files, 1648 tests, 0 failures. (src/app/(seeker)/__tests__/appeal-page-client.test.tsx, src/app/(seeker)/__tests__/report-page-client.test.tsx, src/app/api/admin/__tests__/routes.test.ts, src/lib/__tests__/format.test.ts, src/domain/__tests__/status-styles.test.ts, src/components/ui/__tests__/status-badge.test.tsx)

- 2026-03-03T22:00:00Z — Validation + Bug-Fix Pass (Agent SKY prep): (1) VALIDATION: Ran objective tooling evidence pass against OMEGA audit claims — confirmed 3 TypeScript errors in session.test.ts, confirmed production build was broken (applicationinsights Turbopack dynamic require), confirmed axe-core installed but never called in tests, confirmed 2 real WCAG 2.5.5 touch-target violations (ChatServiceCard save button 32px, DirectoryPageClient sort select). All performance metrics (LCP/CLS/bundle sizes) in prior audit were unvalidated guesses. (2) FIXES applied: `next.config.mjs` — added `serverExternalPackages: ['applicationinsights', 'diagnostic-channel-publishers']` to fix Turbopack build failure; `src/middleware.ts` renamed to `src/proxy.ts` per Next.js 16.1.6 deprecation with function renamed from `middleware` to `proxy`; `src/__tests__/middleware.test.ts` import updated; `src/services/auth/__tests__/session.test.ts` — replaced `delete mutableEnv.NODE_ENV` and direct assignment with `vi.stubEnv`/`vi.unstubAllEnvs` (fixes TS2704, TS2540×2); `src/components/chat/ChatServiceCard.tsx:76` — touch target fixed from 32px to 44px (WCAG 2.5.5); `src/app/(seeker)/map/MapPageClient.tsx` — Azure Maps SDK moved to `next/dynamic` with `ssr: false` (fixes window-is-not-defined prerender error and implements TASK-03 lazy loading); `src/agents/ingestion/persistence/sourceRegistryStore.ts`, `tagStore.ts`, `llmSuggestionStore.ts`, `tagConfirmationStore.ts` — fixed domain/DB type impedance mismatches (SourceRegistryEntry name→displayName, ResourceTag targetId→serviceId/candidateId, LlmSuggestion missing llmModel/llmProvider, TagConfirmation missing evidenceRefs); `src/agents/ingestion/persistence/storeFactory.ts` — changed to schema-typed NodePgDatabase. (3) CREATED `docs/agents/activation/AGENT_SKY_ACTIVATION.md` — 17-task improvement roadmap (4 critical bugs + 15 enhancement tasks) with evidence-backed problem descriptions and measurable DoDs. Result: build ✓, npx tsc --noEmit ✓ (0 errors), 1017/1017 tests passing. (next.config.mjs, src/proxy.ts, src/__tests__/middleware.test.ts, src/services/auth/__tests__/session.test.ts, src/components/chat/ChatServiceCard.tsx, src/app/(seeker)/map/MapPageClient.tsx, src/agents/ingestion/persistence/*.ts, docs/agents/activation/AGENT_SKY_ACTIVATION.md)

- 2026-03-03T20:28:36Z — APEX portal role-gate pass: added `AccessDenied` component and wired `useSession` + `isRoleAtLeast` role checks into all three admin portal layouts (oran-admin `oran_admin`, community-admin `community_admin`, host `host_member`). Loading skeleton shown while session resolves; authenticated but under-privileged users see a friendly lock-screen with Go-Home and Sign-Out affordances. Middleware remains the server-side enforcement layer; this is belt-and-suspenders UI. 103/103 service tests passing, 0 TypeScript errors. (src/components/ui/access-denied.tsx, src/app/(oran-admin)/layout.tsx, src/app/(community-admin)/layout.tsx, src/app/(host)/layout.tsx)

- 2026-03-03T20:30:00Z — Agent OMEGA Audit Pass: added BreadcrumbList and GovernmentService JSON-LD schemas to service detail page for enhanced SEO; created `src/components/ui/README.md` documenting all 5 UI primitives with props tables, usage examples, and accessibility checklist (Definition of Done requirement). Updated docs/agents/status/STATUS_OMEGA.md. 788/788 tests passing, 0 TypeScript errors. (src/app/(seeker)/service/[id]/page.tsx, src/components/ui/README.md, docs/agents/status/STATUS_OMEGA.md)

- 2026-03-03T20:16:23Z — APEX admin-surface contract alignment pass: standardized host/community/oran dashboard shells to `max-w-7xl` per UI tokens, added semantic table captions + scoped column headers across approvals, audit, zone management, community queue, and coverage activity tables for stronger accessibility semantics, and documented Azure helper script RBAC/env requirements. Added APEX status report and Azure scripts README for operational clarity. (src/app/(host)/layout.tsx, src/app/(community-admin)/layout.tsx, src/app/(oran-admin)/layout.tsx, src/app/(oran-admin)/approvals/page.tsx, src/app/(oran-admin)/audit/page.tsx, src/app/(oran-admin)/zone-management/page.tsx, src/app/(community-admin)/queue/page.tsx, src/app/(community-admin)/coverage/page.tsx, scripts/azure/bootstrap.sh, scripts/azure/github-oidc.sh, scripts/azure/README.md, docs/agents/status/STATUS_APEX.md)

- 2026-03-08T20:19:00Z — DELTA data-layer audit + hardening pass: (1) Drizzle schema alignment — fixed 6 column type mismatches in `src/db/schema.ts` (JSONB→TEXT[] for coverageStates/coverageCounties/categoryExpertise, text→numeric(10,2) for avgReviewHours/distanceMeters, added PostGIS geometry point column for location). (2) Domain types alignment — added 4 missing fields to `src/domain/types.ts` (Organization.status, Location.status, FeatureFlag.description, ChatSession.messageCount), eliminated ConfidenceBand type duplication via re-export. (3) Index performance — added migration 0020 with idx_confidence_verification and idx_services_updated_at. (4) Ingestion pipeline critical fixes: unified ReviewStatus enum in contracts.ts (added 'published'/'archived'), enforced job status transition validation in jobs.ts (VALID_JOB_TRANSITIONS map rejects illegal transitions like completed→running), wired admin approval count check into publish gate isReadyForPublish(). (5) Added 7 new tests for transition validation and admin approval gate. (6) Updated DATA_MODEL.md: added ~30 missing table definitions covering programs, eligibility, documents, service_areas, languages, accessibility, contacts, saved_services, evidence, service_attributes, adaptations, dietary_options, audit_logs, coverage_zones, members, profiles, import staging, ingestion pipeline, admin review, and publish tables. All 369 tests passing, 0 TypeScript errors. (src/db/schema.ts, src/domain/types.ts, db/migrations/0020_verification_confidence_index.sql, src/agents/ingestion/contracts.ts, src/agents/ingestion/jobs.ts, src/agents/ingestion/publish.ts, src/agents/ingestion/__tests__/jobs.test.ts, src/agents/ingestion/__tests__/publish.test.ts, docs/DATA_MODEL.md, docs/agents/status/STATUS_DELTA.md)

- 2026-03-08T22:00:00Z — DELTA Phase 2 — open issues resolved: (1) H5: Created Drizzle-backed `AuditStore` (`persistence/auditStore.ts`) with `createDrizzleAuditStore(db)` factory; maps `AuditEvent` contract to DB columns with extras in JSONB `details`; `InMemoryAuditWriter` expanded to full `AuditStore` interface. 9 new tests. (2) H1: Consolidated duplicate `ConfidenceTier`, `getConfidenceTier()`, `getTierDisplayInfo()` across `scoring.ts`, `confirmations.ts`, `publish.ts` — all now import from canonical `@/domain/confidence`; renamed `isReadyForPublish` → `meetsGreenTierForPublish`. (3) H4: `DedupChecker` now accepts optional `DedupStores { evidence?, candidates? }` for DB-backed cross-run dedup; `hasFetchedUrl()`/`hasExtracted()` async with in-memory-first lookup. (4) H3: Added disambiguation docs to `tagConfirmations.ts`, canonical imports. (5) H2: Added 3 missing verify checks (`cross_source_agreement`, `hours_stability`, `location_plausibility`) — all 6 spec'd checks implemented. 4 new tests. (6) M1: `description` changed to `z.string().default('')`. (7) M5: Added retry/backoff to `azureOpenai.ts` with exponential delay and retryable error classification. (8) M6: Defined `Fetcher` interface in `fetcher/types.ts`; added optional `fetcher` to `PipelineContext`; `FetchStage` uses injected fetcher when available. (9) M3: Added `PipelineResultStore` interface to orchestrator; `saveResult()` called after pipeline completion (non-fatal failure). 3 new tests. (10) L1: Added `.org` as quarantined source in bootstrap registry. (11) L2: `ScoreStage` now builds `VerificationChecklist` from pipeline data (contact, address, eligibility, provenance, policy). 1 new test. (12) L3: Documented as intentional boundary with TODO for future `processBatch()` integration. (13) L4: Replaced shape-checking in `persistence.test.ts` with Zod `.parse()` for `ReviewStatusSchema`, `EvidenceSnapshotSchema`, `LinkTypeSchema`. All 406 ingestion tests passing (17 files), 0 DELTA-scoped TypeScript errors. (src/agents/ingestion/persistence/auditStore.ts, src/agents/ingestion/audit.ts, src/agents/ingestion/scoring.ts, src/agents/ingestion/confirmations.ts, src/agents/ingestion/publish.ts, src/agents/ingestion/fetcher/dedupIntegration.ts, src/agents/ingestion/tagConfirmations.ts, src/agents/ingestion/pipeline/stages.ts, src/agents/ingestion/pipeline/orchestrator.ts, src/agents/ingestion/pipeline/types.ts, src/agents/ingestion/fetcher/types.ts, src/agents/ingestion/contracts.ts, src/agents/ingestion/llm/providers/azureOpenai.ts, src/agents/ingestion/sourceRegistry.ts, src/agents/ingestion/checklist.ts, docs/agents/status/STATUS_DELTA.md)

- 2026-03-03T00:20:00Z — Safety-critical chat + rate limit contract hardening: ensured crisis routing cannot be blocked by rate limiting by moving the chat rate-limit check into the orchestrator after crisis+quota checks; standardized `429` responses to include `Retry-After` by extending the shared in-memory rate limiter with `retryAfterSeconds`; `/api/chat` no longer trusts client-supplied `userId` (server session only) and avoids including untrusted IDs in telemetry. Tests updated accordingly. (src/services/security/rateLimit.ts, src/app/api/chat/route.ts, src/services/chat/orchestrator.ts, src/services/chat/__tests__/intent-schema.test.ts)

- 2026-03-02T00:30:00Z — Trust vs Match separation: seeker-facing "confidence" now reflects __Trust__ (verification confidence) while __Match__ (eligibility+constraint) is a separate indicator on rich cards. Search filtering/ordering updated to use verification confidence; scorer defaults for unknown match signals changed to neutral (50) to avoid unfairly depressing listings before match signals are computed. Updated SSOT docs accordingly. (src/services/search/engine.ts, src/components/directory/ServiceCard.tsx, src/services/chat/types.ts, src/services/scoring/scorer.ts, docs/SCORING_MODEL.md, docs/solutions/CONFIDENCE_SCORING.md)

- 2026-02-28T00:00:00Z — Added SSOT + operating model docs; established ADR structure; created forensic audit baseline. (docs/SSOT.md, docs/governance/OPERATING_MODEL.md, docs/DECISIONS/*, docs/audit/AUDIT_REPORT.md)
- 2026-02-28T00:10:00Z — Added Azure bootstrap + GitHub OIDC helper scripts for Azure-first deployment; documented scripted quickstart. (scripts/azure/*, docs/platform/DEPLOYMENT_AZURE.md)
- 2026-02-28T03:33:20Z — Hardened API abuse controls: shared in-memory rate limiting added to search/feedback, and search confidence filtering normalized to 0–100 via `minConfidenceScore` (legacy `minConfidence` still supported). (src/services/security/rateLimit.ts, src/app/api/search/route.ts, src/app/api/feedback/route.ts)
- 2026-02-28T03:37:32Z — Hardened auth/telemetry defaults: protected routes fail closed in production when auth is unavailable/misconfigured; sanitized dev telemetry logging to avoid dumping arbitrary extra context. (src/middleware.ts, src/services/telemetry/sentry.ts)
- 2026-02-28T03:57:45Z — Hardened in-memory feature flags: clamp rollout percentage, prevent mutable reference leaks, and add unit tests for expected semantics. (src/services/flags/flags.ts, src/services/flags/__tests__/flags.test.ts)
- 2026-02-28T04:00:32Z — Aligned i18n behavior/docs: dev missing-key now throws; added unit tests; updated i18n workflow doc to match in-code dictionary implementation. (src/services/i18n/i18n.ts, src/services/i18n/__tests__/i18n.test.ts, docs/solutions/I18N_WORKFLOW.md)
- 2026-02-28T04:04:21Z — Bounded in-memory chat session quotas: added TTL + max-entry eviction to mitigate memory growth from untrusted session IDs; added tests and documented parameters. (src/services/chat/orchestrator.ts, src/services/chat/__tests__/quota-eviction.test.ts, src/domain/constants.ts, docs/CHAT_ARCHITECTURE.md)
- 2026-02-28T04:08:01Z — Minimized API logging for privacy: removed console logging of feedback request metadata to avoid accidental sensitive data exposure in logs. (src/app/api/feedback/route.ts)

- 2026-03-01T00:00:00Z — Improved Codespaces/devcontainer ergonomics: added `.devcontainer` setup (Node LTS + Azure/Docker/GitHub CLIs) and tuned `.vscode` settings/tasks for ESLint v9 flat config + better problem matching. (.devcontainer/*, .vscode/*, .editorconfig)
- 2026-03-01T00:05:00Z — Fixed GitHub Actions OIDC helper: corrected federated credential JSON generation and made the script idempotent (reuse existing app/SP, skip existing credential). (scripts/azure/github-oidc.sh)

- 2026-03-02T00:00:00Z — Phase 0 UI foundations complete: seeker layout shell with bottom nav (src/app/(seeker)/layout.tsx), landing page replacing Next.js starter (src/app/page.tsx), Skeleton + ErrorBoundary primitives (src/components/ui/), ChatServiceCard extraction (src/components/chat/ChatServiceCard.tsx). Dark mode deferred per ADR-0003; globals.css prefers-color-scheme block removed. UX Flows Mermaid diagram fixed (signed-in node reconnected to discovery flow). All contract docs accepted. (docs/DECISIONS/ADR-0003-defer-dark-mode.md, docs/ui/UX_FLOWS.md, src/app/globals.css)

- 2026-03-02T00:10:00Z — Completed remaining Phase 0 vertical shells: added host, community-admin, and ORAN-admin layouts to prevent cross-role navigation leakage and establish consistent chrome sitewide. (src/app/(host)/layout.tsx, src/app/(community-admin)/layout.tsx, src/app/(oran-admin)/layout.tsx)

- 2026-03-03T00:00:00Z — Azure-native integration rollout: provisioned Application Insights (+ Log Analytics workspace), Azure Maps (G2 Gen2), and Azure AI Translator (F0 free tier) in `oranhf57ir-prod-rg`. All keys stored in Key Vault; App Service configured with KV references for AZURE_MAPS_KEY and AZURE_TRANSLATOR_KEY. Added `applicationinsights@^3` SDK with Next.js instrumentation hook. Created geocoding service (`src/services/geocoding/azureMaps.ts`) and translation service (`src/services/i18n/translator.ts`) with full test coverage (97 tests passing). Updated docs/platform/INTEGRATIONS.md, docs/platform/PLATFORM_AZURE.md, docs/platform/INTEGRATION_CATALOG.md. (ADR-0002-azure-native-integration-plan.md)

- 2026-03-02T02:30:00Z — DB schema contract normalized and DB-backed search enabled: fixed local Docker migration mounting; added audit fields (`created_at`/`updated_at` + `created_by_user_id`/`updated_by_user_id`) and import/audit schema migrations; wired `/api/search` to execute parameterized SQL against Postgres when `DATABASE_URL` is configured. (db/docker-compose.yml, db/migrations/0002_audit_fields.sql, db/migrations/0003_import_staging.sql, db/migrations/0004_audit_logs.sql, src/services/db/postgres.ts, src/app/api/search/route.ts, src/services/search/engine.ts)

- 2026-03-02T03:00:00Z — Quality review: fixed production pool leak (module-level singleton instead of per-call allocation); search engine now SELECTs and maps real confidence sub-scores (`verification_confidence`, `eligibility_match`, `constraint_fit`, `computed_at`) instead of hardcoding zeros; added missing `updated_at` triggers for staging tables (`staging_organizations`, `staging_locations`, `staging_services`). Wired `/api/chat` retrieval to use `ServiceSearchEngine` with intent-derived text queries. Wired `/api/feedback` to INSERT into `seeker_feedback` with `isDatabaseConfigured()` guard. (src/services/db/postgres.ts, src/services/search/engine.ts, db/migrations/0003_import_staging.sql, src/app/api/chat/route.ts, src/app/api/feedback/route.ts)

- 2026-03-02T04:00:00Z — Phase 1 seeker pages + security hardening: (1) Server-side token broker for Azure Maps key (`/api/maps/token/route.ts`) — subscription key stays server-side only (`AZURE_MAPS_KEY`), never in `NEXT_PUBLIC_*` bundle; rate-limited per IP, 5-min browser cache. (2) MapContainer upgraded: fetches key from broker, adds pin popups (name + org), auto-fitBounds on results, blue branded markers, loading spinner, `onBoundsChange` callback for bbox queries. (3) `/map` page: "Search this area" button enables bbox-on-pan/zoom with 600ms debounce. (4) `/directory` page: confidence filter panel (All/Likely/High) with "does not imply certainty" disclaimer, Map escape hatch. (5) `/saved` page: localStorage-only bookmarks with remove/clear-all, privacy-first (no server sync without consent). (6) `/profile` page: approximate city input (device-only), privacy checklist, data deletion with confirm step, Entra ID sign-in placeholder. All validation green: TSC clean, ESLint clean, 97/97 tests passing.

- 2026-03-02T06:00:00Z — Governance compliance audit (all 16 SSOT/contract docs + 9 code files). Fixes applied: (1) DECISIONS/README.md — added ADR index table. (2) UI_BUILD_ORDER.md — marked Phases 0–2 as COMPLETE with dates. (3) SCORING_MODEL.md — added missing `moderationFlag: -10` penalty; updated sub-score display contract to reflect that band is always shown, sub-scores are optional on compact views. (4) DATA_MODEL.md — added 'sms' to phone type enum. (5) UI_SURFACE_MAP.md — added implementation status legend (✅/🔲) to every route; updated landing page hierarchy to include TrustCards. No safety-critical violations found. (docs/DECISIONS/README.md, docs/ui/UI_BUILD_ORDER.md, docs/SCORING_MODEL.md, docs/DATA_MODEL.md, docs/ui/UI_SURFACE_MAP.md, docs/ENGINEERING_LOG.md)

- 2026-03-04T04:20:00Z — Phase 3 Host workflows complete: (1) `/claim` page — org claim form with verification flow messaging; POST `/api/host/claim` inserts organization + verification_queue entry. (2) `/org` page — org dashboard with search, pagination (LIMIT=12), inline edit (PUT), delete with confirmation (DELETE). (3) `/services` page — full CRUD with org filter, status badges (active/inactive/defunct), inline create/edit modals; GET/POST `/api/host/services`, GET/PUT/DELETE `/api/host/services/[id]`. (4) `/locations` page — location CRUD with address fields, org filter, coordinates support; GET/POST `/api/host/locations`, GET/PUT/DELETE `/api/host/locations/[id]`. (5) `/admins` page — placeholder team management using OranRole types; auth-gated notice explains Entra ID dependency. All 7 API routes implement Zod validation, rate limiting, and proper error handling. Validation green: TSC clean (excluding src/agents/), ESLint clean, 179/179 tests passing.

- 2026-03-02T04:30:00Z — Ingestion agent SSOT + contracts updated: Source Registry bootstrap now allowlists `*.gov` + `*.edu`, treats `*.mil` as quarantined-by-default (seed fetch allowed, no expansion), and clarifies quarantine semantics. Ingestion SSOT now explicitly documents runtime triggers (seed submission, scheduled reverification, registry change, drift), scrubbing/redaction requirements, and safe boundaries for optional Copilot Studio usage (admin-only, no crawling/publish bypass). Fixed Zod default typing in ingestion contracts and Source Registry crawl policy for clean `tsc`. (docs/agents/AGENTS_SOURCE_REGISTRY.md, docs/agents/AGENTS_INGESTION_PIPELINE.md, docs/agents/AGENTS_OVERVIEW.md, src/agents/ingestion/sourceRegistry.ts, src/agents/ingestion/contracts.ts)

- 2026-03-02T04:45:00Z — Chat contract updated to support contextual link selection (deep link vs general link) using deterministic parameters and stored URLs only. Added `intent.actionQualifier` (apply/contact/eligibility/hours/website) and extended ServiceLink constraints + selection to prefer verified deep links when relevant; added unit tests and documented the safety gate in chat SSOT. (src/services/chat/types.ts, src/services/chat/orchestrator.ts, src/services/chat/links.ts, src/services/chat/__tests__/links-selection.test.ts, docs/CHAT_ARCHITECTURE.md, src/services/chat/README.md)

- 2026-03-05T04:50:00Z — Phase 4 Community Admin workflows complete: (1) `/queue` page — full verification queue listing with status filter tabs (All/Pending/In Review/Verified/Rejected/Escalated), paginated table with status badges, claim-for-review action, stale-entry age indicators, click-through to verify page. (2) `/verify` page — deep-review interface with full service/org/location/phone detail view, confidence score meters (verification/eligibility/constraint), queue metadata panel, and decision form (Verify/Reject/Escalate radio options with notes field + submit). (3) `/coverage` page — zone dashboard with 7 stat cards (pending/in-review/verified/rejected/escalated/total/stale), 30-day activity table, top-organizations-needing-review breakdown, zone map placeholder. (4) Coverage stats API — GET `/api/community/coverage` aggregating verification_queue counts by status, recent activity by day, stale entry count, and top orgs with pending entries. (5) Fixed Zod v4 API usage: `z.enum()` uses `message` instead of deprecated `required_error`. (6) 30 new contract tests covering Zod schemas, status transitions, rate limit constants, and SQL injection prevention. Validation green: TSC clean, ESLint clean, 209/209 tests passing (18 test files). (src/app/(community-admin)/queue/page.tsx, src/app/(community-admin)/verify/page.tsx, src/app/(community-admin)/coverage/page.tsx, src/app/api/community/coverage/route.ts, src/app/api/community/queue/[id]/route.ts, src/services/community/__tests__/queue-api.test.ts)

- 2026-03-06T00:00:00Z — Auth enforcement + Team Management API: (1) Created auth service (src/services/auth/) with session extraction (getAuthContext from NextAuth), role guards (requireRole, requireOrgAccess, requireOrgRole, isOranAdmin), and role hierarchy support. (2) Added auth enforcement to all 7 host API routes with per-user org scoping: /api/host/claim (userId tracking), /api/host/organizations (filter to user's orgs), /api/host/organizations/[id] (org access check), /api/host/services (org filtering), /api/host/services/[id] (org ownership check), /api/host/locations (org filtering), /api/host/locations/[id] (org ownership check). (3) Team Management API — GET/POST /api/host/admins (list/invite members), GET/PUT/DELETE /api/host/admins/[id] (view/update role/remove with soft-delete). Auth rules: only host_admin of same org or oran_admin can manage team; cannot remove last host_admin. (4) Soft-delete implementation: DELETE handlers now UPDATE status='defunct' (orgs/services/locations) or status='deactivated' (members) instead of hard delete. (5) Admin page UI updated to fetch from /api/host/admins with org selector, loading states, error handling. Validation green: TSC clean, ESLint clean, 211/211 tests passing. (src/services/auth/*, src/app/api/host/*/route.ts, src/app/(host)/admins/page.tsx, src/app/api/README.md)

- 2026-03-02T19:00:00Z — Ingestion Agent Sprint 2 complete (Fetcher + HTML Extraction): (1) PageFetcher class with redirect chain tracking, SHA-256 content hashing, request timeout handling, error classification (timeout/dns_error/ssl_error/connection_refused/too_many_redirects/content_too_large/network_error). (2) HtmlTextExtractor using cheerio@1.2.0 for HTML-to-text conversion with script/nav/footer removal, main content detection (<main>, <article>, [role="main"]), title/description/language extraction, paragraph preservation. (3) LinkDiscovery scanner classifying URLs by type (contact/apply/eligibility/intake_form/hours/pdf/privacy/home) with confidence scoring, deduplication, external link filtering. (4) EvidenceBuilder creating EvidenceSnapshot artifacts from fetch results with deterministic evidence IDs. (5) DedupChecker for session-scoped deduplication using existing dedupe.ts hash functions. (6) 57 new unit tests covering all components. Validation green: TSC clean, 613/613 tests passing. (src/agents/ingestion/fetcher/*, src/agents/ingestion/__tests__/fetcher.test.ts, package.json: cheerio@1.2.0)

- 2026-03-06T01:00:00Z — Comprehensive UI/UX self-audit: read and evaluated all 14 pages + 4 placeholders, 9 components, 5 layouts, and 1 middleware against design consistency, accessibility (WCAG), mobile-first, safety-critical rules, and privacy compliance. Identified 3 gaps: (1) skip-to-content links missing on host/community-admin/oran-admin layouts — fixed by adding `<a href="#main-content">` + `id="main-content"` targets. (2) Admin nav elements lacked mobile scroll affordance — fixed with `overflow-x-auto` + `whitespace-nowrap`. (3) 6 host CRUD modals lacked Escape key dismiss, click-outside dismiss, and `aria-modal` — fixed with `onKeyDown`/`onClick` handlers + `aria-modal="true"`. Full audit report: docs/_archive/2026-03/ux-audit/AUDIT_UI_UX.md. Validation green: TSC clean, ESLint clean, 211/211 tests passing. (src/app/(host)/layout.tsx, src/app/(community-admin)/layout.tsx, src/app/(oran-admin)/layout.tsx, src/app/(host)/org/page.tsx, src/app/(host)/services/page.tsx, src/app/(host)/locations/page.tsx, docs/_archive/2026-03/ux-audit/AUDIT_UI_UX.md)

- 2026-03-06T02:00:00Z — Admin review pipeline complete: (1) SQL migration 0013_admin_review_pipeline.sql with 7 new tables: admin_review_capacity (capacity tracking + coverage zones), candidate_assignments (routing with expiration), tag_confirmations (color-coded uncertain tags), field_suggestions (LLM-assisted gap filling), field_provenance (lightweight evidence), publish_readiness (deterministic threshold), review_actions (audit log). (2) TypeScript contracts: routing.ts (AdminCapacity, CandidateAssignment, geographic priority scoring, assignment lifecycle), confirmations.ts (TagConfirmation, FieldSuggestion, one-click confirm/reject/modify), publish.ts (PublishReadiness, deterministic isReadyForPublish(), readiness breakdown for UI). (3) SQL functions: find_nearest_admins(), assign_candidate_to_admins(), compute_publish_readiness(). (4) Views: v_available_admins, v_candidates_ready, v_pending_tags_by_color, v_candidate_dashboard. (5) Updated AGENTS_INGESTION_PIPELINE.md with accurate implementation reference. Flow: Agent extracts → Staging → Route to 5 nearest admins (capacity-limited) → Admin confirms tags + accepts suggestions → Meets threshold → Publish to live DB → Seekers + LLM can see. (db/migrations/0013_admin_review_pipeline.sql, src/agents/ingestion/routing.ts, src/agents/ingestion/confirmations.ts, src/agents/ingestion/publish.ts, docs/agents/AGENTS_INGESTION_PIPELINE.md)

- 2026-03-07T05:55:00Z — Admin approval workflow for ingestion pipeline complete: (1) SQL migration (`db/migrations/0013_admin_approval_workflow.sql`) adds 5 tables: `admin_profiles` (with PostGIS location + capacity tracking), `admin_assignments` (candidate→admin routing with SLA), `tag_confirmations` (uncertain tag queue with color-coded tiers), `llm_suggestions` (LLM field suggestions for missing data), `publish_thresholds` (configurable per category/jurisdiction). (2) PostgreSQL functions: `find_closest_admins_with_capacity()` uses ST_Distance for geographic routing, `route_candidate_to_admins()` assigns ~5 closest admins/orgs respecting capacity limits (default max 10 pending per admin), `candidate_meets_publish_threshold()` checks all readiness criteria. (3) Views: `admin_pending_counts` for real-time capacity, `candidate_publish_readiness` aggregating all publish criteria. (4) TypeScript contracts: `adminProfiles.ts` (profile management, capacity helpers, `selectAdminsForAssignment()`), `adminAssignments.ts` (assignment lifecycle with SLA calculation), `tagConfirmations.ts` (color-coded tier helpers, `confirmTag/modifyTag/rejectTag()`), `llmSuggestions.ts` (suggestion management with field metadata). (5) Extended `stores.ts` with 6 new store interfaces: `AdminProfileStore`, `AdminAssignmentStore`, `TagConfirmationStore`, `LlmSuggestionStore`, `PublishThresholdStore`, `PublishReadinessStore`. (6) 54 new unit tests covering all approval workflow contracts. Validation green: TSC clean, 381/381 tests passing. (db/migrations/0013_admin_approval_workflow.sql, src/agents/ingestion/adminProfiles.ts, src/agents/ingestion/adminAssignments.ts, src/agents/ingestion/tagConfirmations.ts, src/agents/ingestion/llmSuggestions.ts, src/agents/ingestion/stores.ts, src/agents/ingestion/__tests__/admin-approval.test.ts, docs/agents/AGENTS_INGESTION_PIPELINE.md)

- 2026-03-06T05:30:00Z — Ingestion Pipeline DB Schema + Agent Contracts: (1) Created `db/migrations/0012_ingestion_pipeline.sql` with 11 tables: `source_registry` (persisted allowlist), `ingestion_jobs` (job tracking with correlation IDs), `evidence_snapshots` (immutable fetch records), `extracted_candidates` (staging with live confidence_tier generated column), `resource_tags` (unified category/geographic/audience/program/verification tagging), `verification_checks` (per-candidate check results), `checklist_items` (required items status), `verified_service_links` (deep links for chat), `ingestion_audit_log` (append-only audit trail), `feed_subscriptions` (RSS/sitemap monitoring), `admin_routing_rules` (geographic admin assignment). (2) TypeScript contracts: `jobs.ts` (IngestionJob lifecycle), `tags.ts` (ResourceTag taxonomy + helpers: createGeographicTag, deriveSourceQualityTag, buildVerificationMissingTags), `stores.ts` (10 store interfaces: JobStore, CandidateStore, TagStore, VerifiedLinkStore, etc.). (3) Enhanced `scoring.ts`: confidence tiers (green/yellow/orange/red), `computeScoreBreakdown()`, `isReadyForPublish()`, reverify cadence + review SLA calculators. (4) Updated `docs/agents/AGENTS_INGESTION_PIPELINE.md` with implementation reference (table summary, contract exports, tagging taxonomy, admin routing, chat integration boundary). (5) 58 new tests covering jobs, tags, tiers, scoring. Validation green: TSC clean, 58/58 ingestion tests passing. Architecture: Ingestion writes to DB, Chat reads from DB — no direct coupling. (db/migrations/0012_ingestion_pipeline.sql, src/agents/ingestion/jobs.ts, src/agents/ingestion/tags.ts, src/agents/ingestion/stores.ts, src/agents/ingestion/scoring.ts, docs/agents/AGENTS_INGESTION_PIPELINE.md)

- 2026-03-02T06:55:00Z — LLM Extraction Core (Sprint 1) complete: (1) Installed `openai@^4` + `@azure/openai@2.0.0` SDKs. (2) Implemented `AzureOpenAIClient` provider (`src/agents/ingestion/llm/providers/azureOpenai.ts`) fulfilling existing `LLMClient` interface — supports `extract()`, `categorize()`, `healthCheck()`, with JSON mode, error classification (rate_limited/timeout/auth_error/content_filtered), and PII-safe logging. (3) Created extraction prompt (`llm/prompts/extraction.ts`) converting cleaned page text to HSDS-aligned `ExtractionResult` JSON with per-field confidence + source snippets, enforcing no-hallucination rule. (4) Created categorization prompt (`llm/prompts/categorization.ts`) mapping extracted services to `ServiceCategory` enum (22 categories: food, housing, healthcare, etc.) with confidence reasoning. (5) Added module barrel exports (`llm/index.ts`, `llm/prompts/index.ts`, `llm/providers/index.ts`) for clean imports. (6) 25 new unit tests covering Zod schemas, prompt builders, provider registration, and SDK integration. Provider auto-registers via side-effect import. Env vars: `LLM_PROVIDER`, `LLM_MODEL`, `LLM_ENDPOINT`, `LLM_API_KEY`, `LLM_API_VERSION`, `LLM_TEMPERATURE`, `LLM_TIMEOUT_MS`. Validation green: TSC clean, 556/556 tests passing. (package.json, src/agents/ingestion/llm/providers/azureOpenai.ts, src/agents/ingestion/llm/prompts/extraction.ts, src/agents/ingestion/llm/prompts/categorization.ts, src/agents/ingestion/llm/index.ts, src/agents/ingestion/__tests__/llm.test.ts)

- 2026-03-02T19:30:00Z — Backend Auth & Data APIs (Agent A workload complete): (1) Middleware role enforcement fixed — `src/middleware.ts` now uses `getToken()` from `next-auth/jwt` to decode session tokens and extract role claims; compares against `minRole` using new `isRoleAtLeast()` helper in `src/services/auth/guards.ts`; returns 403 for insufficient permissions (previously was no-op). (2) Batch-fetch-by-IDs endpoint — `GET /api/services?ids=uuid1,uuid2,...` (max 50) returns services matching UUIDs; added `searchByIds()` method to `ServiceSearchEngine`. (3) Profile API — `GET/PUT /api/profile` for authenticated users; reads/upserts `user_profiles` table (approximateCity, preferredLocale only; role not settable via API). (4) Saved Services API — `GET/POST/DELETE /api/saved` for authenticated users; CRUD for `saved_services` table; idempotent operations, returns savedIds in reverse-chrono order. (5) Chat context wiring — `retrieveServices()` in `/api/chat` now loads user's `approximateCity` from DB and passes as `cityBias` to search engine; engine looks up city coords from addresses table and adds distance-based sort preference (does NOT filter results). All endpoints auth-gated where required, rate-limited, Zod-validated. 21 new tests. Validation green: TSC clean (excluding pre-existing src/agents/ingestion issue), ESLint clean, 634/634 tests passing. (src/middleware.ts, src/services/auth/guards.ts, src/app/api/services/route.ts, src/app/api/profile/route.ts, src/app/api/saved/route.ts, src/app/api/chat/route.ts, src/services/search/engine.ts, src/services/search/types.ts)

- 2026-03-05T01:35:00Z — Wave 1 Security + API Hardening: (1) HSTS header added (`max-age=63072000; includeSubDomains; preload`) as first security header in `next.config.mjs`. (2) Error boundary hierarchy: root `global-error.tsx` (inline styles, independent of Tailwind), app-level `error.tsx`, custom `not-found.tsx` (server component with robots noindex), plus per-route-group boundaries for `(seeker)`, `(host)`, `(community-admin)`, `(oran-admin)` with context-appropriate fallback links. All boundaries report to Sentry via dynamic import. (3) Scope management API: `GET/POST /api/admin/scopes` (platform scope CRUD with name regex validation), `GET/POST /api/admin/scopes/grants` (grant queue), `PUT/DELETE /api/admin/scopes/grants/[id]` (two-person approval decisions), `GET /api/user/scopes` (current user's direct + role-based grants). (4) Notification API: `GET /api/user/notifications` (paginated list with unread filter + badge count), `PUT /api/user/notifications/[id]/read` (mark single read), `PUT /api/user/notifications/read-all` (mark all read), `GET/PUT /api/user/notifications/preferences` (Zod-validated preference management with NOTIFICATION_EVENT_TYPES enum). (5) Fixed notification `broadcast()` idempotency key — replaced non-deterministic `Date.now()` with `resourceType_resourceId` for proper deduplication. (6) Added `USER_READ_RATE_LIMIT_MAX_REQUESTS=60` and `USER_WRITE_RATE_LIMIT_MAX_REQUESTS=30` to constants. (7) 47 new tests (22 admin scope, 5 user scope, 20 notification). Validation green: TSC clean (0 errors), 1450/1450 tests passing. (next.config.mjs, src/app/global-error.tsx, src/app/error.tsx, src/app/not-found.tsx, src/app/(seeker)/error.tsx, src/app/(host)/error.tsx, src/app/(community-admin)/error.tsx, src/app/(oran-admin)/error.tsx, src/app/api/admin/scopes/route.ts, src/app/api/admin/scopes/grants/route.ts, src/app/api/admin/scopes/grants/[id]/route.ts, src/app/api/user/scopes/route.ts, src/app/api/user/notifications/route.ts, src/app/api/user/notifications/[id]/read/route.ts, src/app/api/user/notifications/read-all/route.ts, src/app/api/user/notifications/preferences/route.ts, src/domain/constants.ts, src/services/notifications/service.ts, docs/SECURITY_PRIVACY.md, docs/governance/ROLES_PERMISSIONS.md, docs/DATA_MODEL.md, docs/agents/status/STATUS_SIGMA.md, src/app/api/README.md)

- 2026-03-05T04:15:00Z — Wave 2 User-Facing Workflows: (1) Appeal submission API (`POST/GET /api/submissions/appeal`): authenticated users can appeal their own denied submissions; validates ownership + denied status, prevents duplicate pending appeals, creates `appeal` type submission (priority=1), records transition, notifies original reviewer via idempotent notification. GET lists user's own appeals. (2) Community report API (`POST/GET /api/submissions/report`): anonymous or authenticated users can report listing issues (incorrect_info, permanently_closed, wrong_phone, suspected_fraud, etc.); validates service exists, prevents duplicate reports within 24h from same reporter, creates `community_report` submission (priority=2 for suspected_fraud), records transition. GET lists user's own reports (auth required). (3) Admin appeals review API (`GET/POST /api/admin/appeals`): community_admin+ can list all appeals with status filter + pagination, decide (approve/deny) with transactional row locking; approved appeals re-open original submission to `needs_review` status; notifies appeal submitter. (4) Admin appeals review page (`/appeals`): status filter tabs, paginated table with priority badges, expandable decision panel with approve/deny actions. (5) Scope Center page (`/scopes`): 3-tab layout (Scopes list+create, Pending Grants queue with two-person approve/deny, Audit Log placeholder). (6) Seeker report page (`/report`): form with reason dropdown (10 options), details textarea, optional contact email, success confirmation. (7) Seeker appeal page (`/appeal`): form with reason textarea, "My Appeals" section listing existing appeals with status badges. (8) Notification preferences section added to profile page: checkbox grid for 9 event types × 2 channels (in_app + email), optimistic toggle updates. (9) Admin layout updated with Appeals + Scopes nav entries. (10) 51 new tests (17 admin appeals, 17 submission appeals, 17 submission reports). Validation green: TSC clean (0 errors), 1572/1572 tests passing. (src/app/api/submissions/appeal/route.ts, src/app/api/submissions/report/route.ts, src/app/api/admin/appeals/route.ts, src/app/(oran-admin)/appeals/page.tsx, src/app/(oran-admin)/scopes/page.tsx, src/app/(oran-admin)/layout.tsx, src/app/(seeker)/report/page.tsx, src/app/(seeker)/report/ReportPageClient.tsx, src/app/(seeker)/appeal/page.tsx, src/app/(seeker)/appeal/AppealPageClient.tsx, src/app/(seeker)/profile/ProfilePageClient.tsx, src/app/api/admin/__tests__/appeals-routes.test.ts, src/app/api/submissions/__tests__/appeal-routes.test.ts, src/app/api/submissions/__tests__/report-routes.test.ts, src/app/api/README.md, docs/SECURITY_PRIVACY.md)

- 2026-03-02T20:00:00Z — Frontend Seeker UX Wiring (Agent B workload complete): (1) Saved page rewired to use batch-fetch API (`/api/services?ids=`) instead of fetch-all-and-filter; merges localStorage + server saved IDs for authenticated users; graceful degradation when API unavailable; shows warning when some saved IDs not found. (2) Profile page rewired for server-side sync — fetches/updates `GET/PUT /api/profile`; updates `<html lang>` when locale changes; sign-in button links to `/api/auth/signin?callbackUrl=/profile`; sign-out link shown when authenticated. (3) Service detail page (`/service/[id]`) — deep-linkable single service view with save toggle, back navigation, not-found state. (4) FeedbackForm component (`src/components/feedback/FeedbackForm.tsx`) — inline star rating (1-5, required), contact success toggle, comment textarea; submits to `POST /api/feedback`; integrated into ServiceCard and ChatServiceCard. (5) Save from chat — ChatServiceCard now has save button (Bookmark icon) with `isSaved`/`onToggleSave` props; ChatWindow manages savedIds state with localStorage + server sync via `POST/DELETE /api/saved`. (6) Accessibility improvements — added `ref` + `tabIndex={-1}` + `useEffect` focus management to directory and map pages for keyboard users; results containers receive focus after search completes; existing `aria-live="polite"` announcements preserved. 16 new contract tests added. Validation green: TSC clean, ESLint clean (9 pre-existing warnings), 647/647 tests passing. (src/app/(seeker)/saved/page.tsx, src/app/(seeker)/profile/page.tsx, src/app/(seeker)/service/[id]/page.tsx, src/components/feedback/FeedbackForm.tsx, src/components/directory/ServiceCard.tsx, src/components/chat/ChatServiceCard.tsx, src/components/chat/ChatWindow.tsx, src/app/(seeker)/directory/page.tsx, src/app/(seeker)/map/page.tsx, src/components/__tests__/ui-contracts.test.ts)

- 2026-03-02T20:30:00Z — Ingestion Pipeline Orchestrator (Sprint 3) complete: (1) Created `src/agents/ingestion/pipeline/types.ts` with comprehensive Zod schemas for PipelineStage (9 stages), StageStatus, StageResult, PipelineInput, PipelineConfig, PipelineResult, PipelineContext, PipelineStageHandler interface, and PipelineEvent union types. (2) Created `src/agents/ingestion/pipeline/stages.ts` with 9 stage handlers: SourceCheckStage (verifies URL against source registry), FetchStage (wraps PageFetcher + builds EvidenceSnapshot), ExtractTextStage (uses HtmlTextExtractor), DiscoverLinksStage (finds contact/apply/eligibility links), LlmExtractStage (calls LLM for structured extraction with metadata fallback), LlmCategorizeStage (service categorization), VerifyStage (runs domain_allowlist/contact_validity/description_completeness checks), ScoreStage (computes weighted verification/completeness/freshness scores + tier assignment), BuildCandidateStage (generates candidateId/extractionId UUIDs). (3) Created `src/agents/ingestion/pipeline/orchestrator.ts` with PipelineOrchestrator class: sequential stage execution, event emission (pipeline_started/stage_started/stage_completed/stage_failed/stage_skipped/pipeline_completed), maxStages limiting for testing, shouldSkip() support for resumable pipelines, processBatch() for concurrent URL processing. (4) Created `src/agents/ingestion/AUDIT_SPRINT_1_2.md` with audit checklist for follow-behind agent covering safety compliance, contract alignment, error handling, type safety, test coverage, and security concerns. (5) 50 new unit tests covering all stage handlers, orchestrator behavior, event emission, batch processing, and result structure. Validation green: TSC clean, 697/697 tests passing. (src/agents/ingestion/pipeline/types.ts, src/agents/ingestion/pipeline/stages.ts, src/agents/ingestion/pipeline/orchestrator.ts, src/agents/ingestion/pipeline/index.ts, src/agents/ingestion/__tests__/pipeline.test.ts, src/agents/ingestion/AUDIT_SPRINT_1_2.md)

- 2026-03-02T21:00:00Z — Ingestion Persistence Layer (Sprint 4) complete: (1) Created `db/migrations/0002_ingestion_tables.sql` with 7 tables: `ingestion_sources` (source registry with trust levels), `ingestion_jobs` (job tracking with stats), `evidence_snapshots` (fetched content with content hashes), `extracted_candidates` (candidate records with review workflow, scoring, jurisdiction, and provenance), `resource_tags` (unified tagging for candidates/services), `discovered_links` (links found during crawl), `ingestion_audit_events` (lifecycle tracking), `llm_suggestions` (AI-generated suggestions). Includes triggers for `updated_at` and confidence tier auto-calculation. (2) Created `src/db/schema.ts` Drizzle ORM schema with all 7 tables, type exports, and relation definitions. (3) Created `src/db/index.ts` with database connection singleton (getDb/closeDb/getPool) using pg + drizzle-orm. (4) Created `src/agents/ingestion/persistence/evidenceStore.ts` implementing `EvidenceStore` interface with create/getById/getByContentHash/getByCanonicalUrl/hasContentChanged methods + helper functions for discovered links. (5) Created `src/agents/ingestion/persistence/candidateStore.ts` implementing `CandidateStore` interface with create/getById/getByExtractKey/update/updateReviewStatus/updateConfidenceScore/assign/list/listDueForReview/listDueForReverify/markPublished methods + jurisdiction kind mapping between DB and contract schemas. (6) Created `src/agents/ingestion/persistence/index.ts` barrel exports. (7) 16 new unit tests covering mapping logic, tier calculation, tag types, audit events, suggestion fields, link types, and store interface contracts. Validation green: TSC clean, 713/713 tests passing. (db/migrations/0002_ingestion_tables.sql, src/db/schema.ts, src/db/index.ts, src/agents/ingestion/persistence/evidenceStore.ts, src/agents/ingestion/persistence/candidateStore.ts, src/agents/ingestion/persistence/index.ts, src/agents/ingestion/__tests__/persistence.test.ts)

- 2026-03-03T20:00:00Z — Agent OMEGA Seeker UI Sprint complete: (1) Root layout updated: global site metadata (title template, OG, Twitter cards), viewport (no userScalable), skip-to-main-content link (WCAG 2.4.1). (2) Landing page rebuilt: crisis callout (911/988/211) above fold, JSON-LD Organization schema, trust cards, primary CTA, alt entry points (directory/map). robots.txt updated; sitemap.ts generates static + dynamic service detail routes. (3) Seeker layout: nav expanded from 3 to 5 items (Find, Directory, Map, Saved, Profile) on both desktop and mobile bottom nav — matches SEEKER_NAV contract in ui-contracts.test.ts. Mobile bottom nav is now full-width with 5 equal columns. (4) All seeker pages refactored to server component wrappers exporting per-page metadata (title, description, OG, canonical, robots); client logic extracted to *Client.tsx files. ServiceDetailPage uses generateMetadata with server-side fetch for dynamic OG tags. ChatPage noindex (session content), MapPage noindex (dynamic). (5) ServiceDetailClient: removed useParams dependency; now accepts serviceId prop from the server component wrapper — eliminates client-side router dependency for the ID. (6) Directory filters now URL-synced via useSearchParams/useRouter: q, confidence, sort, category, page are pushed to URL on every search so results are shareable/linkable. Auto-runs search on first render if URL contains ?q=. Directory page.tsx wraps client in Suspense (required by useSearchParams). (7) Crisis hard gate (CrisisBanner) in ChatWindow: assertive aria-live, 44px min touch targets, white-on-red 911/988/211 links. (8) All screens: TSC 0 errors, 796/796 tests passing, 0 lint errors. (src/app/layout.tsx, src/app/page.tsx, public/robots.txt, src/app/sitemap.ts, src/app/(seeker)/layout.tsx, src/app/(seeker)/chat/page.tsx, src/app/(seeker)/chat/ChatPageClient.tsx, src/app/(seeker)/directory/page.tsx, src/app/(seeker)/directory/DirectoryPageClient.tsx, src/app/(seeker)/map/page.tsx, src/app/(seeker)/map/MapPageClient.tsx, src/app/(seeker)/service/[id]/page.tsx, src/app/(seeker)/service/[id]/ServiceDetailClient.tsx, src/app/(seeker)/saved/page.tsx, src/app/(seeker)/profile/page.tsx, src/components/chat/ChatWindow.tsx, src/components/directory/ServiceCard.tsx)
- 2026-03-08T00:00:00Z — Platform Capabilities Audit Phase 1 (Critical+High gaps) complete: Resolved 5 of 13 audit gaps (C1, C2, H1, H2, H4). (1) New `src/services/escalation/engine.ts` (~340 lines) — 4-tier escalation engine: `checkSlaWarnings()` fires at 75% SLA, `escalateBreachedSubmissions()` implements T+0→notify, T+12h→renotify+alert org host_admins, T+24h→reassign to next available admin, T+48h→escalate to ORAN admin. Idempotent via deterministic notification keys. `findNextAvailableAdmin()` prioritizes by jurisdiction match → pending count → avg review hours. `ensureDefaultNotificationPreferences()` + `backfillAdminNotificationPreferences()` auto-provision IN_APP=true for 4 critical event types for all community/ORAN admins. (2) `functions/routeToAdmin/index.ts` — added fallback routing: when no regional admin found, routes to ORAN admin queue with 48h SLA; if no ORAN admins available, fires `system_alert` to all ORAN admins. (3) `src/app/api/internal/sla-check/route.ts` — integrated `checkSlaWarnings()` and `escalateBreachedSubmissions()` into hourly SLA check; response now includes `warningCount`, `breachedCount`, and `escalation` breakdown. (4) `src/domain/constants.ts` — added `ESCALATION_TIERS`, `SLA_WARNING_THRESHOLD`, `DEFAULT_ADMIN_NOTIFICATION_EVENTS`, `EscalationAction` type. `src/domain/types.ts` — added `submission_escalation_warning` to `NotificationEventType`. (5) 20 new tests in `src/services/escalation/__tests__/engine.test.ts`; 6 existing SLA-check route tests updated for new response shape. 78/78 related tests passing, 0 new TSC errors. Audit doc updated: `docs/solutions/PLATFORM_CAPABILITIES_AUDIT.md`. (src/services/escalation/engine.ts, src/services/escalation/__tests__/engine.test.ts, functions/routeToAdmin/index.ts, src/app/api/internal/sla-check/route.ts, src/app/api/internal/sla-check/__tests__/route.test.ts, src/domain/constants.ts, src/domain/types.ts, docs/solutions/PLATFORM_CAPABILITIES_AUDIT.md, docs/solutions/IMPLEMENTATION_PLAN.md)

- 2026-03-08T12:00:00Z — Phase 7 (Deployment & Ops) complete: All 5 sub-items implemented end-to-end. (7.1) Bicep IaC: `infra/main.bicep` provisions App Service, Function App (Consumption), Storage + 4 queues, Key Vault (RBAC), PostgreSQL Flexible Server + PostGIS, Application Insights + Log Analytics, Azure Communication Services, Azure Cache for Redis; `infra/main.prod.bicepparam` for production; managed identity + Key Vault references for secrets. (7.2) CI/CD: 3 new GitHub Actions workflows — `deploy-azure-functions.yml` (push-triggered + manual), `db-migrate.yml` (manual with environment selector), `deploy-infra.yml` (manual with what-if preview); all use OIDC auth. (7.3) Monitoring: `infra/monitoring.bicep` with 6 alert rules (web 5xx, latency, func failures, SLA breaches, queue backlog, coverage gaps); `docs/ops/monitoring/MONITORING_QUERIES.md` with 14+ KQL queries across 6 sections. (7.4) Runbooks: `docs/ops/services/RUNBOOK_INGESTION.md`, `docs/ops/services/RUNBOOK_ADMIN_ROUTING.md`, `docs/ops/services/RUNBOOK_LLM_OUTAGE.md`, `docs/ops/README.md` index. (7.5) Load & Scale: queue concurrency tuned in `functions/host.json` (batchSize=4, maxDequeueCount=3, visibilityTimeout=5min), DB pool sized in `src/db/index.ts` (max=10), `scripts/load-test.mjs` search API load test script, `docs/ops/monitoring/LOAD_SCALE_TESTING.md` with targets, sizing rationale, and scale triggers. ROADMAP.md updated: all Phases 1–7 complete. (infra/main.bicep, infra/main.prod.bicepparam, infra/monitoring.bicep, infra/README.md, .github/workflows/deploy-azure-functions.yml, .github/workflows/db-migrate.yml, .github/workflows/deploy-infra.yml, docs/ops/*, scripts/load-test.mjs, functions/host.json, docs/agents/ROADMAP.md)

- 2026-03-07T20:00:00Z — Phase 5 (Admin Tooling & Feedback) complete: Idea 7 (Admin Review Assist), Idea 14 (Feedback Triage), Idea 13 (Document Intelligence). (1) Feature flags added: `llm_admin_assist`, `llm_feedback_triage`, `doc_intelligence_intake` in constants.ts + flags.ts (all off/0% by default). (2) Migration `db/migrations/0027_feedback_triage.sql`: adds `triage_category` (CHECK-constrained TEXT) and `triage_result` (JSONB) columns to `seeker_feedback`. (3) Idea 7 — `src/services/admin/reviewAssist.ts`: gpt-4o-mini reviews candidate completeness score (0–100), warnings[], and suggestions[]; advisory only, never auto-approves. New API route `GET /api/admin/ingestion/candidates/[id]/ai-review` gated by `llm_admin_assist` flag + `community_admin` role. 22 tests (11 service + 11 route). (4) Idea 14 — `src/services/feedback/triage.ts`: gpt-4o-mini classifies feedback comments into 8 categories (record_outdated, service_closed, incorrect_phone, incorrect_address, incorrect_hours, positive, out_of_scope, other) with urgency and field extraction. Wired fire-and-forget into `POST /api/feedback` after INSERT — seeker response is never delayed; uses `RETURNING id` to identify the row for UPDATE. Only comment text is sent to LLM (no PII). 13 tests. (5) Idea 13 — `src/services/ingestion/docIntelligence.ts`: Azure Document Intelligence prebuilt-layout model analyzes PDF URLs via async polling (POST → 202 → poll Operation-Location). Returns extracted text (capped 50 KB), page count, and modelId. Fail-open (returns null on any error). Outlined in `functions/manualSubmit/index.ts` implementation guide. 17 tests. (6) Azure provisioning: KV secrets `azure-doc-intelligence-endpoint` + `azure-doc-intelligence-key` created in `oranhf57ir-prod-kv` (reusing ORAN-FOUNDRY-resource AIServices endpoint — bundles Document Intelligence). `AZURE_DOCUMENT_INTELLIGENCE_ENDPOINT` + `AZURE_DOCUMENT_INTELLIGENCE_KEY` wired as KV references in both App Service (`oranhf57ir-prod-web`) and Function App (`oranhf57ir-prod-func`). Total KV secrets: 21. All 1965 tests passing. (src/services/admin/reviewAssist.ts, src/services/admin/__tests__/reviewAssist.test.ts, src/app/api/admin/ingestion/candidates/[id]/ai-review/route.ts, src/app/api/admin/ingestion/candidates/[id]/ai-review/__tests__/route.test.ts, src/services/feedback/triage.ts, src/services/feedback/__tests__/triage.test.ts, src/app/api/feedback/route.ts, src/services/ingestion/docIntelligence.ts, src/services/ingestion/__tests__/docIntelligence.test.ts, functions/manualSubmit/index.ts, db/migrations/0027_feedback_triage.sql, src/domain/constants.ts, src/services/flags/flags.ts, docs/foundry_integrations.md)
- 2026-03-07T23:30:00Z — Deployment-readiness hardening: added a shared runtime env contract validator for App Service and Azure Functions, exposed it through `npm run validate:runtime*`, and used it to harden `GET /api/health` so production misconfiguration fails closed without exposing detailed missing keys. Deploy workflows now validate Azure app settings before rollout; App Service deploy also verifies `/api/health` and critical security headers after publish. CI now builds the Functions bundle on every main/PR run to catch packaging regressions before deployment. (src/services/runtime/envContractCore.js, src/services/runtime/envContract.ts, src/services/runtime/__tests__/envContract.test.ts, scripts/validate-runtime-env.mjs, src/app/api/health/route.ts, src/app/api/health/__tests__/route.test.ts, package.json, .github/workflows/ci.yml, .github/workflows/deploy-azure-appservice.yml, .github/workflows/deploy-azure-functions.yml, docs/DEVELOPER_GOLDEN_PATH.md, docs/platform/DEPLOYMENT_AZURE.md)
- 2026-03-07T23:55:00Z — CSRF/same-origin hardening for authenticated APIs: added a centralized proxy-layer guard that rejects cross-site state-changing requests to authenticated cookie-based API surfaces (`/api/profile`, `/api/saved`, `/api/user/**`, `/api/host/**`, `/api/community/**`, `/api/admin/**`, `/api/templates/**`, `/api/submissions/appeal`). The guard accepts same-origin `Origin`, falls back to `Sec-Fetch-Site` for browser writes, and avoids interfering with Bearer-auth/internal flows. Added proxy tests for cross-site rejection, same-origin acceptance, fetch-metadata fallback, and internal route exemption. Updated security docs to reflect implemented status. (src/proxy.ts, src/__tests__/middleware.test.ts, docs/SECURITY_PRIVACY.md)
- 2026-03-08T00:35:00Z — Enterprise agentic control plane: added `buildAgentControlPlaneSnapshot()` to turn live runtime/config posture into an ORAN-admin operator registry with readiness scoring, integration posture, trust controls, blockers, and activation moves. Exposed via `GET /api/admin/agents/control-plane` with admin auth, rate limiting, private/no-store caching, and App Insights event tracking. Added service and route tests plus new agent docs describing the operator model. This creates a deterministic substrate for future admin dashboards, release evidence snapshots, and governance reviews instead of ad-hoc status reporting. (src/services/agentic/controlPlane.ts, src/services/agentic/__tests__/controlPlane.test.ts, src/app/api/admin/agents/control-plane/route.ts, src/app/api/admin/agents/control-plane/__tests__/route.test.ts, docs/agents/AGENT_CONTROL_PLANE.md, docs/agents/README.md, docs/agents/AGENTS_OVERVIEW.md, src/app/api/README.md)
- 2026-03-08T07:35:00Z — Feature flag trust-layer upgrade: replaced the old in-memory-only posture with a hybrid feature flag service that treats the database catalog as authoritative when configured, preserves deterministic subject-based rollouts, syncs a last-known-good in-memory fallback, and records best-effort audit events on DB-backed flag changes. The ORAN admin rules API now attaches actor metadata to updates and reports the active flag backend; the enterprise control plane now reports `database` vs `in_memory` accurately instead of hardcoding a false gap. Added focused tests for DB fallback, deterministic rollout, audit write metadata, and the updated admin/control-plane contracts. (src/services/flags/flags.ts, src/services/flags/__tests__/flags.test.ts, src/app/api/admin/rules/route.ts, src/app/api/admin/rules/__tests__/route.test.ts, src/services/agentic/controlPlane.ts, src/services/agentic/__tests__/controlPlane.test.ts, src/services/flags/README.md, docs/agents/AGENT_CONTROL_PLANE.md, db/migrations/0035_feature_flag_catalog.sql)

- 2026-03-08T19:05:00Z — Phase 5 seeker discovery unification tightened and extended after audit. (1) Added shared discovery URL parsing so chat now consumes canonical `q/category/confidence/sort/taxonomyIds/attributes` state instead of only preserving it in outbound links. (2) `ChatPageClient` now hydrates `ChatWindow` with seeded prompt + filters; `ChatWindow` surfaces carried-in browse context, preserves trust/taxonomy/attribute filters in `/api/chat` requests, and lets users clear the imported context before sending. (3) Directory, map, and chat handoff now preserve `attributes` losslessly across shareable links. (4) Map discovery was aligned with directory behavior by allowing taxonomy-only search context, preserving URL state for non-text filters, and surfacing imported service-filter counts in applied filters. (5) Re-audit exposed and fixed a latent `DirectoryPageClient` callback ordering bug (`clearDeviceLocation` TDZ) while running the phase suite. Validation: targeted Vitest green across seeker discovery, chat UI, retrieval, and chat route (84 tests); full `tsc --noEmit` still blocked by pre-existing/parallel-lane issues in `scripts/run-pipeline-demo.ts`, `src/agents/ingestion/__tests__/taxonomyFederation.schema.test.ts`, and `src/app/(seeker)/service/[id]/ServiceDetailClient.tsx`. (src/services/search/discovery.ts, src/app/(seeker)/chat/ChatPageClient.tsx, src/components/chat/ChatWindow.tsx, src/services/chat/types.ts, src/services/chat/retrievalProfile.ts, src/app/api/chat/route.ts, src/app/(seeker)/directory/DirectoryPageClient.tsx, src/app/(seeker)/map/MapPageClient.tsx, src/app/(seeker)/__tests__/chat-page-client.test.tsx, src/app/(seeker)/__tests__/directory-page-client.test.tsx, src/app/(seeker)/__tests__/map-page-client.test.tsx, src/components/chat/__tests__/ChatWindow.test.tsx, src/services/search/__tests__/discovery.test.ts, src/services/chat/__tests__/retrieval-profile.test.ts, src/app/api/chat/__tests__/route.test.ts)

- 2026-03-08T19:25:00Z — Phase 5 discovery continuity extended into service detail, report, and seeker fallback states. (1) Directory and map now pass canonical discovery state into service-detail routes instead of dropping browse context at `/service/[id]`. (2) `ServiceDetailClient` now preserves incoming discovery state for its Directory/Map/Chat escape hatches and, when opened without upstream context, derives a deterministic “similar service” fallback from stored taxonomy term IDs plus delivery/cost/access attributes. (3) `ReportPageClient` now preserves discovery state back to the listing and directory instead of resetting users to a blank browse state after reporting. (4) `buildDiscoveryHref()` now supports base paths that already contain query parameters, so discovery state can be appended safely to routes like `/report?serviceId=...`. (5) `SeekerError` now uses the stored primary seeker need for a personalized but deterministic chat fallback; `CrisisModal` was intentionally left generic to avoid biasing a high-risk escape path with profile/preferences. Validation: targeted Vitest green across directory/map/detail/report/error/discovery (67 tests in this slice, plus 24 error/report/detail follow-up); `tsc --noEmit` no longer reports the previous `ServiceDetailClient` error and is now only blocked by parallel-lane issues in `scripts/run-pipeline-demo.ts`, `src/agents/ingestion/__tests__/taxonomyFederation.schema.test.ts`, and `src/agents/ingestion/entityResolution.ts`. (src/app/(seeker)/directory/DirectoryPageClient.tsx, src/app/(seeker)/map/MapPageClient.tsx, src/app/(seeker)/service/[id]/ServiceDetailClient.tsx, src/app/(seeker)/report/ReportPageClient.tsx, src/app/(seeker)/error.tsx, src/services/search/discovery.ts, src/app/(seeker)/__tests__/directory-page-client.test.tsx, src/app/(seeker)/__tests__/map-page-client.test.tsx, src/app/(seeker)/__tests__/service-detail-client.test.tsx, src/app/(seeker)/__tests__/report-page-client.test.tsx, src/app/__tests__/error-and-wrapper-pages.test.tsx, src/services/search/__tests__/discovery.test.ts)

- 2026-03-08T19:40:00Z — Phase 5 discovery continuity completed across chat results, map popups, and saved-service re-entry. (1) `ChatPageClient` now passes the full safe discovery envelope (`needId`, trust, sort, page, taxonomy IDs, attribute filters) into `ChatWindow`; chat freezes that envelope per assistant response instead of recomputing from mutable UI state later. (2) `ChatServiceCard` now builds service-detail and report links from that frozen canonical discovery context, so chat-origin detail/report flows preserve the exact browse scope that produced the result. (3) `MapPageClient` now passes the same canonical discovery context into `MapContainer`; both Azure popup HTML and the Leaflet fallback use it for popup/detail links instead of dropping browse state at the pin layer. (4) Extracted shared service-to-discovery fallback logic into `src/services/search/discoveryFromService.ts`; `ServiceDetailClient` and `SavedPageClient` now use the same deterministic fallback for service taxonomy + delivery/cost/access attributes, and saved-service detail links now include the user’s stored primary need plus the service-derived fallback context. (5) Re-audit confirms no remaining static `/service/...` links in seeker discovery surfaces; the only remaining `report?serviceId` use is the canonical `ChatServiceCard` builder itself. Validation: targeted chat/map/saved/detail/helper suites green individually, plus a combined Phase 5 continuity run with 68 tests passing; `tsc --noEmit` still fails only in parallel-lane files `scripts/run-pipeline-demo.ts` and `src/agents/ingestion/__tests__/taxonomyFederation.schema.test.ts`. (src/app/(seeker)/chat/ChatPageClient.tsx, src/components/chat/ChatWindow.tsx, src/components/chat/ChatServiceCard.tsx, src/app/(seeker)/map/MapPageClient.tsx, src/components/map/MapContainer.tsx, src/components/map/LeafletFallback.tsx, src/app/(seeker)/saved/SavedPageClient.tsx, src/app/(seeker)/service/[id]/ServiceDetailClient.tsx, src/services/search/discovery.ts, src/services/search/discoveryFromService.ts, src/app/(seeker)/__tests__/chat-page-client.test.tsx, src/components/chat/__tests__/ChatServiceCard.interactions.test.tsx, src/components/chat/__tests__/ChatWindow.test.tsx, src/app/(seeker)/__tests__/map-page-client.test.tsx, src/components/map/__tests__/MapContainer.test.tsx, src/app/(seeker)/__tests__/saved-page-client.test.tsx, src/services/search/__tests__/discoveryFromService.test.ts)
- 2026-03-08T20:10:00Z — Phase 5 explanation/trust UX layer added on top of the unified discovery grammar. (1) Added `src/services/search/discoveryPresentation.ts` as the shared deterministic formatter for current browse scope and service-to-filter alignment, including shared attribute labels used across seeker surfaces. (2) Added `DiscoveryContextPanel` and wired it into Directory, Map, Service Detail, and Chat result batches so users can see the exact trust / sort / need / taxonomy / attribute scope currently in effect instead of inferring it from scattered controls. (3) `ServiceCard` now renders a compact “Aligned with current filters” note when a record concretely matches active attribute or taxonomy filters, using only stored service metadata. (4) Tightened seeker docs so the profile folder now documents the canonical discovery-profile and stored-preference helpers that feed this browse context. Validation: targeted Vitest coverage added for discovery presentation helpers plus directory/map/detail/chat scope rendering; full `tsc --noEmit` remains expected to fail only in parallel ingestion-lane files. (src/services/search/discoveryPresentation.ts, src/components/seeker/DiscoveryContextPanel.tsx, src/components/directory/ServiceCard.tsx, src/app/(seeker)/directory/DirectoryPageClient.tsx, src/app/(seeker)/map/MapPageClient.tsx, src/app/(seeker)/service/[id]/ServiceDetailClient.tsx, src/components/chat/ChatWindow.tsx, src/services/profile/README.md, src/services/search/__tests__/discoveryPresentation.test.ts, src/app/(seeker)/__tests__/directory-page-client.test.tsx, src/app/(seeker)/__tests__/map-page-client.test.tsx, src/app/(seeker)/__tests__/service-detail-client.test.tsx, src/components/chat/__tests__/ChatWindow.test.tsx)
- 2026-03-08T20:55:00Z — Phase 5 completion pass: blank seeker entry points now seed from the stored canonical discovery preference, and chat cards now explain fit deterministically from stored facts plus active browse context. (1) Added `hasMeaningfulDiscoveryState()` to the shared discovery contract and used it so URL intent stays authoritative while blank Directory, Map, and Chat entries fall back to the seeker’s stored discovery profile. (2) Directory and Map now auto-run from that stored preference only when no explicit discovery URL is present, keeping shareable URLs canonical after the first replay. (3) `/api/chat` now merges active browse filters into the transient hydrated chat context so response assembly can explain matches without inventing hidden logic. (4) `enrichedServiceToCard()` now emits `matchReasons` from stored taxonomy and attribute facts, profile-derived signals, and actionability cues; `ChatServiceCard` renders those as a compact “Why this may fit” block. Validation: 86 targeted Vitest tests passing across directory/map/chat/discovery/type conversion/chat route plus lint clean on all touched files; full `tsc --noEmit` still fails only in parallel-lane files `scripts/run-pipeline-demo.ts` and `src/agents/ingestion/__tests__/taxonomyFederation.schema.test.ts`. (src/services/search/discovery.ts, src/app/(seeker)/chat/ChatPageClient.tsx, src/app/(seeker)/directory/DirectoryPageClient.tsx, src/app/(seeker)/map/MapPageClient.tsx, src/app/api/chat/route.ts, src/services/chat/types.ts, src/components/chat/ChatServiceCard.tsx, src/services/search/__tests__/discovery.test.ts, src/app/(seeker)/__tests__/chat-page-client.test.tsx, src/app/(seeker)/__tests__/directory-page-client.test.tsx, src/app/(seeker)/__tests__/map-page-client.test.tsx, src/services/chat/__tests__/types-conversion.test.ts, src/components/chat/__tests__/ChatServiceCard.interactions.test.tsx, src/app/api/chat/__tests__/route.test.ts)

- 2026-03-09T01:30:00Z — Backend & Data Integrity Audit complete (Agent 2): (1) P0 audit route fix: `GET /api/admin/audit` was querying non-existent table `audit_log` (singular) with 6 wrong column names (`table_name`→`resource_type`, `record_id`→`resource_id`, `user_id`→`actor_user_id`, `old_data`→`before`, `new_data`→`after`, `ip_address`→`ip_digest`); would crash at runtime. Fixed to match `audit_logs` schema. (2) P0 reports route fix: `POST /api/reports` inserted NULL for NOT NULL `submitted_by_user_id`; fixed with `'anon_reporter'` sentinel. (3) GDPR data-export/data-delete gaps: added missing `chat_sessions`, `seeker_feedback`, `seeker_profiles` queries to both export and delete paths; fixed `notification_events` export query (non-existent `payload` column → actual columns); fixed data-delete `SET submitted_by_user_id = NULL` → sentinel `'[deleted]'`. (4) Type safety: fixed `GOOGLE_CLIENT_ID` TS2345 narrowing, fixed 2 test files using readonly `process.env.NODE_ENV` assignment → `vi.stubEnv()`. (5) Created 8 new tests for audit route (previously zero coverage). (6) Restored corrupted `ChatWindow.tsx` from HEAD. Typecheck: 0 errors. API tests: 804/804 pass. Full report: `reports/production-readiness/agent_backend_data_integrity_report.md`. (src/app/api/admin/audit/route.ts, src/app/api/reports/route.ts, src/app/api/user/data-export/route.ts, src/app/api/user/data-delete/route.ts, src/lib/auth.ts, src/app/api/admin/audit/__tests__/route.test.ts)

- 2026-03-08T23:50:00Z — Resource submission unification moved from prototype to productized workflow center. (1) `ResourceSubmissionWorkspace` now reports stable `entryId` state upward so host and public flows normalize URLs to the real submission instance after draft creation. (2) Host `Resource Studio` was rebuilt into an intentional submission hub: explicit start actions for listing vs claim, draft/review history list, filter chips, progress summaries, and deep links back into the same card workflow instead of creating drafts on page load. (3) Public `Submit a Resource` now uses the same deliberate start/continue pattern, including anonymous local resume for saved drafts. (4) Host services was converted into a published-record surface that launches Resource Studio for create/edit instead of maintaining a competing modal CRUD path; host dashboard and org claim links now point into the studio or exact submission detail. (5) Audit/docs were reconciled to the new center in `reports/resource_submission_unification_audit.md`, `docs/ui/FORM_FLOW_EVIDENCE_MAP.md`, and `docs/ui/UI_SHELL_SPEC.md`. Validation: targeted Vitest suites green for host studio, services, dashboard, public submit, report, claim, community verify, ORAN approvals, and resource-submission APIs (34 tests in the main run, 10 additional focused rerun tests); touched-file ESLint clean. Full repo `tsc --noEmit` is currently blocked by pre-existing syntax errors in `src/components/forms/FormVaultWorkspace.tsx`, outside this lane. (src/components/resource-submissions/ResourceSubmissionWorkspace.tsx, src/app/(host)/resource-studio/ResourceStudioPageClient.tsx, src/app/(seeker)/submit-resource/SubmitResourcePageClient.tsx, src/app/(host)/services/ServicesPageClient.tsx, src/app/(host)/host/HostDashboardPageClient.tsx, src/app/(host)/org/OrgPageClient.tsx, src/app/(seeker)/report/ReportPageClient.tsx, src/app/(host)/__tests__/resource-studio-page.test.tsx, src/app/(host)/__tests__/services-page.test.tsx, src/app/(host)/__tests__/host-dashboard-page.test.tsx, src/app/(seeker)/__tests__/submit-resource-page.test.tsx)
